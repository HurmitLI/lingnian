# 「聆年 · 家庭记忆传家宝」第一阶段开发手册

> 阶段名称：核心纵向切片  
> 配套文档：`niannian.md`、`通用技术栈手册.md`、`技术适配声明.md`  
> 文档状态：开发前评审稿  
> 边界：本文只说明第一阶段如何开发与验收；本次不执行开发。

## 一、阶段目标

### 1.1 要交付什么

第一阶段只打通一条可以亲手操作、可以中断恢复、可以保留证据的最小链路：

```text
创建一个测试老人档案
→ 从一个人生阶段获得一个温和问题
→ 录制或上传一段测试音频
→ 本地语音转写
→ 人工校对转写
→ 模型按原话整理为故事草稿
→ 子女确认或退回修改
→ 归档故事并形成一条时间线记录
→ 刷新页面或重启服务后仍能查看
```

第一阶段的目标不是做出“完整传家宝”，而是验证四件最关键的事：

1. 老人和子女能否顺利完成一次语音回忆采集；
2. 本地转写结果是否达到“可以人工校对”的最低可用程度；
3. 模型能否忠于口述整理，而不补写不存在的史实；
4. 音频、转写、确认和故事归档能否可靠保存并在中断后恢复。

### 1.2 阶段产物

- 可在本机打开的最小 Web 纵向切片；
- 一个可创建、读取的测试老人档案；
- 一个温和回忆问题入口；
- 浏览器录音/音频上传、本地转写和人工校对；
- 模型整理草稿与人工确认；
- 一条可查看的归档故事及时间线；
- 持久化任务状态和失败重试入口；
- mock 自动化测试与真实端到端冒烟记录；
- README、`.env.example`、数据库迁移和本阶段验收记录。

### 1.3 第一阶段验收标准

- 能用虚构档案创建一位老人，并看到保存成功；
- 能完成一次录音或上传，系统不覆盖原始音频；
- 本地 ASR 能产出转写；听不清或无法判断的内容不会被自动脑补；
- 子女可以编辑转写，原始转写仍可追溯；
- 模型整理稿与校对稿之间的新增事实能被识别或明确标为“待核实”；
- 只有点击“确认归档”后，故事和时间线才进入正式档案；
- 点击“跳过”后，该话题不产生故事归档，临时内容按规则清理；
- 页面刷新、后端重启后，已确认记录和任务状态仍然存在；
- 无 Key、错误音频、超限文件、ASR 失败、模型超时都有可理解的错误提示，不显示程序堆栈；
- 自动化测试通过；真实 ASR 冒烟通过；主模型无真实 Key 时必须明确标为“待验”，不能用 mock 代替。

### 1.4 明确不做

- 不做完整家谱、几代关系图和健康档案；
- 不做照片触发、OCR、老物件识别；
- 不做每周提醒、生日/纪念日调度；
- 不做 30～50 个故事的覆盖度目标和完整人生阶段看板；
- 不生成完整 Markdown/PDF 回忆录；
- 不做声音克隆、照片开口、数字人或视频合成；
- 不做微信、企业微信、小程序、短信入口；
- 不做家庭成员邀请、正式登录、老人离世后的权限继承；
- 不做云备份、对象存储、生产部署或公开访问；
- 不做 Chroma/RAG、外部任务队列、微服务或多 Agent；
- 未完成加密与恢复设计前，不录入真实老人隐私资料。

---

## 二、技术适配摘要

- **开发路径**：纵向切片。
- **后端**：Python 3.11 + FastAPI + Pydantic + pytest。
- **最小前端**：Next.js + TypeScript + Tailwind CSS；它是正式交互方向的第一条切片，不另做一次性静态调试页。
- **持久化**：SQLite + SQLAlchemy + Alembic；音频文件保存在本地受控目录。
- **长任务**：转写与模型整理采用“提交任务 + 查询状态 + 获取结果”，任务状态写入数据库；暂不引入消息队列。
- **编排**：固定代码状态机；模型不自由选择删除、分享或付费工具。
- **ASR**：本地 FunASR/Paraformer；开工时固定模型与版本并验证 Apple Silicon 兼容性。
- **主模型**：第一阶段推荐通义千问云 API，只用虚构测试文本做真实冒烟；通过后端适配层调用并遵守《技术适配声明》的数据边界。没有 Key 时用 mock 开发，但最终冒烟保持待验。
- **端口**：暂定前端 `3011`、后端 `8011`，开工时再次检查。

---

## 三、技术栈与模型

### 3.1 第一阶段组件

| 层 | 组件 | 用途 |
|---|---|---|
| 后端 | Python 3.11.x、FastAPI、Pydantic | API、状态机、数据校验、服务编排 |
| 数据 | SQLite、SQLAlchemy、Alembic | 多实体关联、任务恢复、schema 迁移 |
| 测试 | pytest | 业务、错误、恢复、解析器和安全测试 |
| 前端 | Next.js、TypeScript、Tailwind CSS | 老人档案、录音/上传、转写校对、确认归档、时间线 |
| ASR | FunASR + Paraformer 中文模型 | 本地语音转写；具体版本开工时锁定 |
| 音频 | FFmpeg | 在保留原文件的前提下生成 ASR 用的 16 kHz 单声道 WAV 副本 |
| 主模型 | 通义千问云 API + 可替换的兼容适配层 | 第一阶段只用虚构测试文本生成一个温和问题、忠实整理故事；不得直接写正式档案 |

### 3.2 模型调用契约

第一阶段只允许两种文本模型调用：

1. **回忆问题生成**：输入最少量的测试档案信息、人生阶段、已跳过的提示 ID；输出一个问题和选择理由，不输出多个问题轰炸老人。
2. **故事整理**：输入人工校对后的转写；输出标题、忠实整理正文、可能的时间信息、人物引用和待核实项。

模型名称、`base_url`、超时、重试次数、温度等都从后端配置读取。开发文档不能把当时可用的云模型名永久硬编码；开工当日应从所选厂商的官方控制台锁定一个稳定模型名，并记录在验收报告中。

### 3.3 不引入的组件

第一阶段不引入 Chroma、OCR、Celery/RQ、Redis、对象存储、PostgreSQL、WebSocket、SSE、Docker、声音克隆和视频生成 SDK。

---

## 四、环境与配置

### 4.1 开工前环境准备

- 使用 `/Users/hurmit/.local/bin/python3.11` 创建独立 `.venv`，不得使用系统 Python 3.9；
- 使用 Node 22 LTS 的隔离环境，不覆盖本机其他项目当前使用的 Node 24；
- 安装并验证 FFmpeg 后再开发音频标准化；
- 检查 `3011`、`8011` 端口，冲突时选择新的空闲端口并更新 README；
- 经确认后初始化 Git，并在生成任何数据或密钥文件前写好 `.gitignore`；
- 开发开始后锁定依赖版本，提交与包管理器匹配的锁文件。

### 4.2 配置项

`.env.example` 只写变量名和安全示例，不含真实秘密。建议至少包含：

| 配置项 | 含义 | 是否秘密 |
|---|---|---|
| `APP_ENV` | 本地/测试环境 | 否 |
| `BACKEND_PORT` | 后端端口，暂定 8011 | 否 |
| `FRONTEND_ORIGIN` | 允许访问后端的前端地址 | 否 |
| `DATABASE_URL` | 本地 SQLite 路径 | 否，但不得指向用户其他数据库 |
| `ASSET_ROOT` | 受控资产根目录 | 否 |
| `ASR_MODEL_ID` | 锁定的本地 ASR 模型 | 否 |
| `LLM_PROVIDER` | 所选模型提供方或 local/mock | 否 |
| `LLM_MODEL` | 开工时锁定的模型名 | 否 |
| `LLM_BASE_URL` | 模型服务地址 | 否 |
| `LLM_API_KEY` | 模型密钥 | **是**，只放本地 `.env`/Secret |
| `LLM_TIMEOUT_SECONDS` | 超时 | 否 |
| `MAX_AUDIO_BYTES` | 暂定上传上限，可配置 | 否 |

上传大小和时长上限是工程保护参数，不是已经确认的产品规则。开发时可先设置保守临时值，并在验收记录中显式标注，不能把临时值写成最终业务承诺。

### 4.3 数据使用限制

- 第一阶段验收使用虚构人物资料和自录/授权测试音频；
- 不使用真实老人健康、身份证明、住址、联系方式等资料；
- 未确认云模型数据边界时，真实转写不得发送给任何外部模型；
- 日志只记录任务 ID、阶段、耗时、模型标识、Token/错误类型，不记录完整转写和音频内容。

---

## 五、项目结构

开发时建议创建下列最小结构；没有职责的空目录不预先创建：

```text
念念/
├── backend/
│   ├── app/
│   │   ├── api/                 # /api/v1 路由
│   │   ├── core/                # 配置、日志、错误、安全路径
│   │   ├── models/              # SQLAlchemy 模型
│   │   ├── schemas/             # Pydantic 请求、响应、模型输出
│   │   ├── services/
│   │   │   ├── asr/             # 音频标准化与本地 ASR
│   │   │   ├── llm/             # 主模型适配、重试、mock
│   │   │   ├── archive/         # 故事、时间线、媒体关联
│   │   │   └── workflow/        # 固定状态机与任务恢复
│   │   ├── prompts/              # 可版本追踪的 Prompt
│   │   └── main.py
│   ├── alembic/
│   ├── tests/
│   └── pyproject.toml            # 或 requirements.txt，开工时二选一
├── frontend/                     # 第一条正式 Web 纵向切片
├── data/                         # 被 .gitignore 排除
│   ├── db/
│   ├── assets/original/
│   ├── assets/derived/
│   └── quarantine/
├── docs/
├── .env.example
├── .gitignore
└── README.md
```

PRD、通用手册、技术适配声明和本手册应复制或整理进 `docs/`，但不得删除当前根目录原文件。

---

## 六、数据、资产与状态

### 6.1 数据实体

第一阶段只实现完成最小链路所需的字段；不提前补齐全产品数据模型。

| 实体 | 关键字段 | 说明 |
|---|---|---|
| `family_archives` | `id`、`display_name`、`schema_version`、时间戳 | 第一阶段只建一个测试家庭档案容器 |
| `people` | `id`、`family_id`、`role`、`display_name` | 记录老人/子女角色，不做完整账户系统 |
| `elder_profiles` | `person_id`、出生年份/年代、籍贯、职业摘要、偏好称呼 | 只收集触发测试问题所需的最少字段，不收健康信息 |
| `memory_sessions` | `id`、`elder_id`、`life_stage`、`prompt_id`、`status` | 一次回忆采集工作流 |
| `media_assets` | `id`、业务归属、类型、受控路径、MIME、大小、SHA-256、状态 | 原始音频与派生音频分开登记 |
| `transcripts` | `id`、`session_id`、`raw_text`、`corrected_text`、ASR 元信息、版本 | 原始转写不可被人工稿覆盖 |
| `story_drafts` | `id`、`session_id`、标题、正文、待核实项、模型信息、状态 | 模型产物必须待人工确认 |
| `stories` | `id`、`elder_id`、确认后的标题/正文、来源稿 ID、确认人、确认时间 | 只有确认后生成 |
| `timeline_events` | `id`、`story_id`、时间表达、标准化时间（可空）、可信状态 | 不确定时间保留原话，不强行推断日期 |
| `workflow_tasks` | `id`、类型、状态、进度、尝试次数、错误码、输入/输出引用 | 转写和整理任务可恢复 |
| `consent_events` | `id`、动作、主体、对象、时间 | 记录确认/跳过/删除动作，不保存被删敏感正文 |

### 6.2 资产规则

- 上传文件先进入隔离区，通过格式、真实 MIME、大小和文件头检查后再进入正式目录；
- 文件名使用系统生成的 UUID，不信任用户文件名，不允许 `../` 或绝对路径；
- 原始音频只读保存，派生 WAV 单独保存，任何转码都不得覆盖原件；
- 数据库保存相对受控路径，不保存任意本机绝对路径；
- 每个文件保存 SHA-256，验收时可以检查文件是否损坏或被替换；
- 清理临时文件前检查业务归属；正式故事引用的资产不得被临时清理任务误删。

### 6.3 状态机

```text
DRAFT_PROFILE
→ PROMPT_READY
→ RECORDING_PENDING
→ AUDIO_UPLOADED
→ TRANSCRIBING
→ TRANSCRIPT_REVIEW
→ ORGANIZING
→ DRAFT_REVIEW
→ CONFIRMED
→ ARCHIVED
```

允许的旁路：

- 任意未归档状态 → `SKIPPED`：停止追问，不生成故事；
- `TRANSCRIBING` / `ORGANIZING` → `FAILED_RETRYABLE`：保存错误码，可人工重试；
- `DRAFT_REVIEW` → `TRANSCRIPT_REVIEW`：退回修订后重新整理；
- 已归档内容的删除不在第一阶段 UI 中提供，避免在继承和审计规则未确定前草率实现。

状态转换由后端确定性代码控制。前端不能直接把记录改成 `ARCHIVED`，模型也不能自行调用确认或删除。

### 6.4 重启恢复

- `TRANSCRIBING`、`ORGANIZING` 在进程异常退出后标为可恢复/可重试，不能永远卡在“处理中”；
- 已上传但未转写的音频可继续提交；
- 已生成但未确认的草稿继续停留在人工确认页；
- 已确认的故事与时间线刷新/重启后不丢失；
- 数据库迁移前后均要有可回滚或备份说明。

---

## 七、API / 工具设计

所有接口统一使用 `/api/v1`，返回受约束结构。错误统一为：

```json
{
  "error": {
    "code": "AUDIO_TYPE_NOT_ALLOWED",
    "message": "这段音频格式暂不支持，请换一个文件再试。",
    "request_id": "..."
  }
}
```

### 7.1 最小接口清单

| 方法与路径 | 作用 | 关键校验/结果 |
|---|---|---|
| `POST /api/v1/families` | 创建测试家庭容器 | 显示名长度、重复提交幂等 |
| `POST /api/v1/elder-profiles` | 创建老人测试档案 | 最少字段、年份范围、家庭归属 |
| `GET /api/v1/elder-profiles/{id}` | 读取档案 | 不返回内部路径或秘密 |
| `POST /api/v1/memory-sessions` | 创建一次回忆会话 | 老人、人生阶段、问题来源 |
| `POST /api/v1/memory-sessions/{id}/skip` | 跳过当前问题 | 确定性改状态，停止后续处理 |
| `POST /api/v1/memory-sessions/{id}/audio` | 录音/文件上传 | MIME、文件头、大小、安全文件名、归属 |
| `POST /api/v1/memory-sessions/{id}/transcription-tasks` | 提交本地转写 | 返回持久化 `task_id`，不让请求无限等待 |
| `GET /api/v1/tasks/{task_id}` | 查询任务状态 | `queued/running/succeeded/failed_retryable/failed_final` |
| `PATCH /api/v1/transcripts/{id}` | 保存人工校对稿 | 版本控制，原始转写不覆盖 |
| `POST /api/v1/memory-sessions/{id}/organization-tasks` | 提交故事整理 | 只读取校对稿，返回 `task_id` |
| `GET /api/v1/story-drafts/{id}` | 查看整理草稿 | 返回来源版本、待核实项、模型信息 |
| `POST /api/v1/story-drafts/{id}/confirm` | 人工确认归档 | 幂等；创建故事和时间线，记录确认动作 |
| `POST /api/v1/story-drafts/{id}/reject` | 退回修改 | 不产生正式故事 |
| `GET /api/v1/elder-profiles/{id}/timeline` | 查看归档时间线 | 只返回已确认记录 |

### 7.2 工具权限

| 工具 | 第一阶段权限 |
|---|---|
| `create_person_profile` | 人工提交后执行 |
| `capture_memory` | 可生成一个问题；不得读取无关家庭资料 |
| `transcribe_voice` | 只处理本会话已验证的本地音频 |
| `archive_story` | **必须在人工确认后**由确定性代码执行 |
| `build_timeline` | 只基于已确认故事；不确定日期允许为空 |
| `query_archive` | 第一阶段只查询当前测试老人已确认故事 |
| `attach_media`、`build_relation_map`、`generate_memory_book`、`synthesize_keepsake`、`share_keepsake` | 第一阶段不开放 |

---

## 八、Prompt 设计

### 8.1 `memory_question_v1`

- **角色**：口述史采访助手；温和、一次一个点、不预设答案。
- **输入**：偏好称呼、人生阶段、已经问过的提示 ID、已跳过提示 ID、最少量背景。
- **输出**：`question`、`reason`、`safety_check`。
- **约束**：只输出一个开放式问题；禁止再次提出已跳过话题；禁止把推测写进问题；禁止追问健康、创伤等第一阶段未授权敏感信息。
- **校验**：Pydantic 强校验数量与字段；失败有限重试；仍失败使用经过审核的本地题库兜底。

### 8.2 `story_organizer_v1`

- **角色**：忠实编辑，不是小说作者，不是史实补全者。
- **输入**：人工校对稿、问题、可选的明确元信息。
- **输出**：`title`、`body`、`timeline_mentions[]`、`people_mentions[]`、`uncertainties[]`、`source_coverage`。
- **约束**：不得新增原文没有的人名、地点、日期、因果或情绪；听不清和矛盾内容保留为待核实；正文必须能回溯到校对稿。
- **正例方向**：调整口语重复、分段和标点，但不改变事实。
- **禁止反例**：把“那几年”改成具体年份；把“可能去过上海”改成“在上海工作”；替老人补写感受。
- **确定性校验**：实体/数字差异扫描、空输出、超长、字段缺失检查。检测到新增事实时进入 `DRAFT_REVIEW` 并突出显示，不自动归档。

### 8.3 证据分层

系统必须保留四层内容：

```text
原始音频（证据）
→ ASR 原始转写（机器结果）
→ 人工校对稿（人确认的口述文本）
→ 模型整理稿（待确认的表达优化）
```

任何后一层都不能覆盖前一层。正式故事必须记录来源版本和确认人。

---

## 九、第一阶段最小界面

这是可延续的正式 Web 纵向切片，但视觉范围被严格限制为核心链路：

1. **测试档案页**：填写最少档案信息；明确提示当前只用测试资料。
2. **回忆问题页**：一次只显示一个问题；有明显的“跳过”按钮。
3. **录音/上传页**：录制、重录、上传、播放确认；显示文件状态，不自动上传无确认的录音。
4. **转写校对页**：显示原始转写和可编辑校对区；待核实/听不清内容醒目标注。
5. **故事确认页**：并排或差异方式查看校对稿与模型稿；可以确认、退回，不默认确认。
6. **时间线页**：只显示已确认故事；可以播放关联测试音频。
7. **任务状态**：排队、处理中、失败可重试、成功都有明确状态；刷新后继续显示。

第一阶段不做完整品牌视觉、复杂首页、营销页、家谱画布、视频时间轴、多人后台和多个终端适配。关键页面至少验证桌面宽度与 390 px 手机宽度，保证按钮、字体和录音操作可用。

---

## 十、测试要求

### 10.1 第一层：mock 自动化测试

后端至少覆盖：

- 档案必填、非法年份、跨家庭访问被拒绝；
- 状态机只允许合法转换，前端不能直接归档；
- 重复提交确认不会生成两条故事；
- 跳过后不再进入转写/整理/归档；
- 上传伪装格式、超限文件、危险文件名、路径遍历被拒绝；
- 原始音频不会被派生文件覆盖，SHA-256 正确登记；
- ASR 成功、空结果、超时、进程失败、重试耗尽；
- 原始转写和人工校对稿版本分离；
- 模型输出缺字段、格式异常、超时、SDK 初始化失败走统一错误；
- 故事整理新增日期、人名或地点时被标记待核实；
- 错误响应和日志不包含堆栈、Key、完整转写；
- 服务重启后未完成任务进入可恢复状态；
- 数据库迁移可在空库和已有测试库执行。

前端至少覆盖：

- 录音权限拒绝、上传失败、任务失败、重试、空状态；
- 校对内容未保存时离开页面有提示；
- 未确认不能在时间线看到故事；
- 跳过按钮可发现、可操作且不会继续追问；
- 390 px 宽度下关键控件不溢出、不遮挡。

### 10.2 第二层：真实端到端冒烟

使用一位虚构老人资料和一段明确授权的 1～3 分钟测试录音，至少完成：

```text
真实音频
→ FFmpeg 生成派生音频
→ 真实本地 FunASR 转写
→ 人工校对
→ 真实主模型整理
→ 人工确认
→ SQLite/本地资产持久化
→ 刷新与重启后查看故事和时间线
```

记录但暂不预设通过数值：

- 机器、ASR 模型和版本；
- 音频时长、转写耗时、资源占用；
- 转写中需要人工修正的典型问题；
- 主模型厂商、模型名、首响应/总耗时、Token 与估算费用；
- 首次结构合规与重试后结构合规；
- 是否出现新增事实、遗漏、错误时间推断；
- 任务失败和重启恢复结果。

没有真实模型 Key 时，允许完成代码与 mock 测试，但这一条必须标记为“主模型真实冒烟待验”，第一阶段不能宣称完全验收通过。

### 10.3 恢复与安全专项

- 转写处理中强制停止后端，重启后任务可识别并重试；
- 草稿待确认时刷新页面，内容和状态不丢；
- 拷贝一份测试数据后校验哈希，损坏时给出可理解提示；
- 检查前端构建产物、Git 暂存区和日志中没有 Key、原始音频或完整转写；
- 服务只绑定本机或明确受控地址，第一阶段不得直接暴露公网。

---

## 十一、产品经理验收清单

开发完成后，产品经理按下面顺序亲自操作：

- [ ] 打开本机验收地址，看到“第一阶段仅使用测试资料”的提示。
- [ ] 创建一位虚构老人档案，刷新页面后资料仍在。
- [ ] 选择一个人生阶段，只看到一个温和问题。
- [ ] 点击“跳过”，确认系统不再追问、时间线没有新增故事。
- [ ] 重新开始一次回忆，录制或上传一段 1～3 分钟测试音频，并能播放确认。
- [ ] 提交转写，看到明确的处理中状态；刷新页面后任务没有消失。
- [ ] 查看转写结果，把一处错字改正确并保存。
- [ ] 查看模型整理稿，确认它没有偷偷增加人名、年份、地点、因果或感受。
- [ ] 如果存在听不清或不确定内容，确认页面明确标为待核实，而不是自动补全。
- [ ] 点击“退回”，确认故事没有进入时间线。
- [ ] 再次整理并点击“确认归档”，确认时间线出现一条故事。
- [ ] 重启服务后再次打开，确认档案、故事、音频关联和任务状态仍在。
- [ ] 上传一个错误格式文件，看到普通人能理解的提示，没有代码堆栈。
- [ ] 在手机宽度查看关键页面，确认文字、按钮、录音和确认操作不被遮挡。
- [ ] 查看验收报告，确认 mock 测试、真实 ASR、真实主模型三项分别写明“通过/未通过/待验”，没有混用。

满足全部适用项后，第一阶段才可进入验收通过状态。

---

## 十二、风险与待确认项

| 风险/问题 | 第一阶段处理 |
|---|---|
| 方言和老人含混语音准确率未知 | 不预先承诺；先用授权样本记录差错，再决定是否引入讯飞/腾讯云方言 ASR |
| 云模型可能接触家庭转写 | 开发前确认数据边界；未确认时只传虚构测试文本 |
| 静态加密与恢复密钥未定 | 第一阶段不录入真实家庭资料；第二阶段优先完成 |
| Apple Silicon 上 FunASR/依赖兼容性 | 开工时做最小安装和一分钟音频基准，再锁版本 |
| 浏览器录音格式差异 | 保留原件，以 FFmpeg 生成统一派生格式；Safari/Chrome 至少各验证一个 |
| 模型“润色”导致事实漂移 | 证据分层、实体/数字差异检查、人工确认；模型永远不能直接归档 |
| 端口已被占用 | 使用 3011/8011 或新的空闲端口，不停止其他项目服务 |

开发前需要产品经理确认的重大项与《技术适配声明》一致：主模型数据边界、最终首发终端、加密恢复责任和第一阶段测试数据限制。

---

## 十三、交接给下一阶段

第一阶段验收通过后，第二阶段可以直接复用：

- 已验证的老人档案、回忆会话、媒体、转写、故事、时间线和任务数据契约；
- 固定工作流状态机与恢复机制；
- 本地 ASR 管线、音频资产规则和模型适配层；
- 原始证据 → 人工校对 → 模型草稿 → 人工确认的内容安全链；
- 第一条 Next.js 交互切片，不另建平行临时前端；
- 自动化测试、真实冒烟记录和验收流程。

第二阶段再扩展家谱/关系、完整人生阶段题库、照片触发、敏感话题记忆、定时提醒、文字回忆录，以及真实数据启用前必须完成的加密、备份和恢复方案。

---

## 十四、开发启动条件

本手册完成不代表已经开始开发。只有同时满足以下条件后，才进入第一阶段实现：

- 产品经理确认《技术适配声明》没有偏离产品目标；
- 确认第一阶段使用虚构/授权测试资料；
- 确认主模型的数据边界和 Key 提供方式（无 Key 可先开发，最终冒烟待验）；
- 确认允许初始化 Git、创建项目目录、虚拟环境和安装依赖；
- 如需安装 FFmpeg 或 Node 22 隔离环境，先说明影响再执行。
