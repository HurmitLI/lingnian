# 念念 · 第二阶段开发手册

> 阶段名称：本地 MVP 完整化与安全硬化  
> 基线分支：`第一阶段分支`，提交 `02b2edf`  
> 开发分支：`第二阶段分支`  
> 配套文档：`niannian.md`、`技术适配声明.md`、`第一阶段开发手册.md`  
> 文档状态：三项产品决定已确认，进入开发

---

## 一、阶段目标

第二阶段把第一阶段的单次回忆纵向切片扩展为可以在本机长期使用的家庭记忆 MVP：

```text
安全解锁家庭档案
→ 建立人物和家谱关系
→ 用人生阶段、照片或老物件触发回忆
→ 记住已讲内容、偏好和不愿再谈的话题
→ 安排温和提醒
→ 汇总已确认故事
→ 生成可本地保存的文字回忆录
→ 备份并完成恢复演练
```

阶段完成门槛不是“页面更多”，而是：在完成加密与恢复验收后，可以使用明确授权的家庭资料进行本机长期积累，且不会因服务重启、设备迁移或误触提醒而破坏隐私和老人意愿。

---

## 二、本阶段范围

### 2.1 必须完成

1. 家庭人物与家谱/关系维护；
2. 七个人生阶段的可维护问题题库与覆盖度；
3. 照片、老物件图片上传、归属、哈希校验和故事关联；
4. 照片/老物件触发回忆，但不自动编造图片背景；
5. 跨会话记忆：已确认事实、人物、时间表达、讲述偏好和阶段覆盖；
6. 敏感话题与“不要再问”规则；
7. 本机持久化提醒、暂停、跳过和幂等补偿；
8. 文字回忆录预览、Markdown 导出和 PDF 导出；
9. 家庭档案静态加密、密钥恢复包、备份和恢复演练；
10. 读取媒体时校验 SHA-256，损坏或缺失时给出普通人可理解的提示；
11. 第一阶段已有数据的向前迁移和回滚验证；
12. 自动化测试、真实模型边界测试、浏览器验收和恢复验收。

### 2.2 不属于第二阶段

- 正式公网部署、多用户登录和远程协作；
- 微信、小程序或短信的正式消息推送；
- 声音复刻、照片驱动视频和数字人；
- 老人离世后的自动权限继承；
- 正式云备份、公开分享和跨家庭权限；
- OCR 自动识别、RAG/向量数据库和外部任务队列；
- 把模型生成内容直接写成史实或绕过人工确认。

---

## 三、已确认的三项产品决定

| 决定 | 推荐默认方案 | 影响 |
|---|---|---|
| 真实转写能否发送给通义千问 | **默认不发送**。只有用户对单次内容明确授权后，才发送人工校对稿；未授权时允许手工整理或后续接本地模型 | 决定真实资料能否使用云端整理能力 |
| 恢复密钥由谁保管 | **用户保管恢复包**。主密钥存入当前 Mac 钥匙串；另导出一次性恢复包，由家庭自行离线保存；系统不留后门 | 恢复包丢失且原 Mac 钥匙串不可用时，资料无法解密 |
| 第二阶段提醒渠道 | **只做应用内本机提醒**，不接微信/短信；第三阶段确定正式终端后再接外部渠道 | 不产生对外发送、平台审核和短信费用 |

三项方案已由产品经理于 2026-08-29 确认。实现必须遵守表中默认值；后续若改变云数据边界、恢复责任或提醒渠道，需要新增决策记录和回归验收，不能静默改动。

---

## 四、安全与数据边界

### 4.1 数据分级

| 级别 | 示例 | 默认处理 |
|---|---|---|
| 测试 | 虚构档案、系统合成音频 | 可用于自动化和模型冒烟 |
| 授权非敏感 | 家庭明确授权、无健康/证件/财务等敏感内容 | 本机加密保存；发送云模型仍需单次授权 |
| 授权敏感 | 健康、创伤、家庭冲突、证件、精确住址等 | 本机加密；默认禁止发送云模型和进入提醒 |
| 禁止处理 | 未获授权的第三方隐私、账号凭据 | 拒绝保存或立即安全删除 |

### 4.2 加密方案

- 采用应用层信封加密，不把“文件位于本机”误称为加密；
- 文本敏感字段和媒体文件使用 AES-256-GCM，每个对象使用独立随机 nonce；
- 主密钥不进入 SQLite、`.env`、日志、Git、备份正文或前端；
- 当前 Mac 使用系统钥匙串保存主密钥引用；
- 恢复包使用用户设置的恢复口令加密，并包含版本、KDF 参数和校验信息；
- 不记录恢复口令，不提供绕过恢复口令的后门；
- 加密前保留第一阶段测试数据备份，迁移成功并验收前不删除原数据；
- 所有解密失败统一返回安全错误，不输出密钥、明文或堆栈。

### 4.3 云模型出口

- 默认只把人工校对后的最小必要文本发送给模型；
- 原始音频、照片、家庭关系、健康信息和恢复数据不发送；
- 每次调用记录模型名、内容级别、授权事件、输入哈希、结果状态和 Token，不记录完整明文；
- 未获得对应数据级别授权时，后端必须拒绝云调用，不能仅靠前端隐藏按钮；
- 模型结果仍须经过事实差异检查和人工确认。

---

## 五、数据模型增量

第二阶段在现有表上新增或扩展以下实体：

| 实体 | 用途 |
|---|---|
| `person_relationships` | 人物之间的父母、子女、配偶、兄弟姐妹及自定义关系 |
| `question_prompts` | 七阶段问题题库、启停状态、敏感级别和版本 |
| `topic_preferences` | 喜欢聊、谨慎询问、不要再问及来源 |
| `memory_facts` | 仅来自人工确认故事的跨会话事实索引 |
| `media_links` | 照片/老物件与人物、故事、回忆会话的关系 |
| `reminders` | 本机提醒时间、状态、最后发送和幂等键 |
| `memory_books` | 回忆录版本、章节、导出文件、哈希和生成状态 |
| `encryption_metadata` | 密钥版本、算法版本和迁移状态，不保存密钥 |
| `backup_manifests` | 备份版本、资产清单、哈希、创建时间和恢复结果 |
| `model_consent_events` | 云模型数据级别、授权人、授权范围和撤销记录 |

迁移必须支持：旧库升级、升级失败回滚、测试副本恢复；不得直接在唯一数据库上试验破坏性迁移。

---

## 六、固定工作流

### 6.1 回忆触发

```text
选择老人
→ 读取阶段覆盖、已确认事实、已跳过话题和偏好
→ 选择“题库 / 照片 / 老物件”入口
→ 生成一个问题
→ 敏感话题后端过滤
→ 用户确认愿意继续
→ 进入第一阶段已验证的采集链路
```

照片触发时，模型只能询问“这张照片让您想起什么”，不能推断照片中的人物、地点、年代或事件。用户明确标注的照片信息与模型推测必须分层保存。

### 6.2 跨会话记忆

- 只从人工确认归档的故事提取长期事实；
- 原始 ASR、未确认草稿和模型推测不进入长期记忆；
- 时间不确定时保留原表达，不自动换算具体年份；
- 人物同名或关系冲突进入待核实队列，不自动合并；
- “不要再问”优先级高于题库、提醒和模型建议。

### 6.3 提醒

- 只创建本机应用内提醒；
- 一个提醒只对应一个老人和一个主题；
- 跳过、暂停或设置为敏感后自动取消相关提醒；
- 服务重启后补偿检查，但同一幂等键不得重复提醒；
- 未回应只温和提醒一次，随后自动暂停。

### 6.4 文字回忆录

- 只使用已确认故事；
- 按人生阶段或人工章节排序；
- 每段可以追溯到故事、人工校对稿和媒体；
- Markdown 为最低可恢复格式，PDF 为派生文件；
- PDF 生成失败不得阻塞 Markdown 导出；
- 每个版本保留清单和哈希，旧版本不被静默覆盖。

---

## 七、开发顺序

### 里程碑 A：安全底座

- 数据分级与云模型授权门；
- 密钥生成、钥匙串、恢复包和加密服务；
- 敏感文本与媒体的加密迁移；
- 媒体读取哈希校验；
- 备份清单和恢复演练工具。

### 里程碑 B：家庭档案完整化

- 人物 CRUD 与关系图；
- 七阶段题库、覆盖度和题目版本；
- 敏感话题与不要再问；
- 仅从已确认故事生成跨会话记忆。

### 里程碑 C：多入口采集

- 照片/老物件上传、预览、关联和删除；
- 题库、照片、老物件三种触发入口；
- 模型问题的事实边界和敏感过滤；
- 第一阶段采集链路复用，不重建平行流程。

### 里程碑 D：长期积累与回忆录

- 本机提醒和重启补偿；
- 档案完整度、阶段覆盖度与故事清单；
- 回忆录章节、预览、Markdown/PDF 导出；
- 备份、恢复和导出失败降级。

---

## 八、验证要求

### 8.1 自动化

- 所有第一阶段测试继续通过；
- 每个新增 API 包含成功、权限拒绝、输入错误和幂等测试；
- 加密 round-trip、错误密钥、篡改密文、密钥轮换和恢复包测试；
- 媒体缺失、哈希不一致和路径逃逸测试；
- 敏感话题过滤、跳过后不再提问和提醒取消测试；
- 仅已确认故事进入长期记忆和回忆录测试；
- 旧数据库升级、降级和备份恢复测试。

### 8.2 人工验收

- 新建三代虚构人物并建立关系，刷新和重启后不丢；
- 把一个话题设为“不要再问”，确认题库、照片触发和提醒都不会再次提出；
- 上传照片并触发一个不预设照片事实的问题；
- 完成两个不同人生阶段故事，看到覆盖度变化；
- 创建提醒、重启服务并确认不会重复；
- 生成 Markdown 与 PDF 回忆录，内容只来自已确认故事；
- 复制备份到新目录，用恢复包恢复并核对数据库与资产哈希；
- 修改一份媒体文件，确认系统报告损坏而不是继续播放；
- 检查日志、Git 和导出包中没有 Key、恢复口令或未授权明文；
- 在用户明确授权与未授权两种情况下，分别验证云模型允许和拒绝路径。

---

## 九、第二阶段完成定义

同时满足以下条件才可建立 `第二阶段分支` 的验收快照：

1. 第一阶段回归测试全部通过；
2. 本阶段新增自动化测试全部通过；
3. 三项产品决定已经写入文档和配置默认值；
4. 加密、恢复包、备份和恢复演练通过；
5. 真实授权资料模式默认不会把内容发送到云模型；
6. 家谱、题库、照片触发、跨会话记忆、敏感话题和提醒可用；
7. Markdown/PDF 回忆录可生成、可追溯、可本地保存；
8. 用户完成本阶段人工验收；
9. `.env`、数据库、媒体、模型、密钥和恢复包均未进入 Git；
10. 更新 README、迁移说明和第二阶段验收记录。
