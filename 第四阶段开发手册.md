# 念念 · 第四阶段开发手册

> 阶段名称：本机视频 + 原声留声  
> 分支：`第四阶段分支`  
> 配套文档：`niannian.md`、`技术适配声明.md`、`通用技术栈手册.md`、`第三阶段开发手册.md`  
> 状态：开发与用户验收已完成

## 一、阶段目标

在不上传家庭素材、不克隆声线、不让照片“开口说话”的前提下，把已经人工确认的故事、原始录音和照片在这台 Mac 上合成一份可播放、可下载、可长期保存的 MP4 数字念想。

### 完成定义

- 家人只能选择已人工确认且有原始录音的故事。
- 生成前必须完成一次性授权，授权与本次故事清单哈希绑定，不能复用到其他素材。
- 合成全程本机完成，不调用云模型，新增平台费用为 0 元。
- 输出为 H.264 + AAC 的 720p MP4，画面明确标注“家庭记忆整理 · 原始录音”。
- 有照片时使用原照片作为背景；没有照片时生成本地故事卡，不生成虚构人像。
- 生成任务可轮询、可刷新恢复、失败可重试，服务中断后不会伪装成完成。
- 真实资料模式下，视频与授权人标识继续使用现有家庭主密钥保护。
- 下载前重新验证尺寸和 SHA-256，文件损坏时阻止播放和下载。
- 后端、迁移、前端、端到端、真实本地 FFmpeg 合成和浏览器验收通过。

### 本阶段明确不做

- 声音克隆、声线仿冒、TTS 代读老人没有说过的话；
- 照片驱动口型、数字人、生成式人脸动画；
- 任何付费声音或视频 API，以及把音频、照片、视频上传给第三方；
- 公开分享链接、微信发送、多用户登录、远程权限和继承规则；
- 公网部署、对象存储、云数据库、正式监控；这些仍属于第五阶段。

## 二、技术适配摘要

- 开发路径：沿用现有纵向切片，新增“选故事 → 明确授权 → 本机合成 → 预览/下载”。
- 复用第三阶段的正式 Web 外壳、集中 API 客户端、持久化长任务、轮询、恢复、媒体播放和手机布局。
- 视频编码使用项目已锁定的 `imageio-ffmpeg 0.6.x` 内置 FFmpeg，不要求全局安装 Homebrew FFmpeg。
- 故事卡使用 Pillow 和 macOS 本地中文字体生成，不下载商业字体。
- 固定编排用代码状态机，不使用 LLM 自由规划视频。
- 长耗时处理继续采用“提交任务 + 查询状态 + 获取结果”，不引入外部队列。

## 三、技术栈与配置

| 能力 | 选择 | 用途 |
|---|---|---|
| API / 任务 | FastAPI + BackgroundTasks | 创建、轮询、重试本地合成任务 |
| 持久化 | SQLite + SQLAlchemy + Alembic | 授权、版本、清单、进度、错误和文件元数据 |
| 图像排版 | Pillow | 照片适配、标题与原声标识 |
| 视频编码 | imageio-ffmpeg 内置 FFmpeg 7.x | H.264/AAC MP4、faststart、音频拼接 |
| 媒体保护 | AES-256-GCM 流式媒体加密 | 真实资料模式下的视频静态加密 |
| 前端 | 现有 Next.js + TypeScript | 选择、授权、进度、预览和下载 |

新增不含秘密的配置：

- `KEEPSAKE_MAX_STORIES=10`
- `KEEPSAKE_MAX_SOURCE_SECONDS=1800`
- `KEEPSAKE_WIDTH=1280`
- `KEEPSAKE_HEIGHT=720`
- `KEEPSAKE_FFMPEG_TIMEOUT_SECONDS=900`

本阶段不需要新 API Key、新账号或新付费订阅。

## 四、数据、授权与状态

### 4.1 `keepsake_authorizations`

- `id`、`elder_id`、`actor_label`、`story_ids`、`manifest_sha256`；
- `original_voice_authorized`：已确认有权使用所选原始录音；
- `private_family_use`：只用于家庭记忆；
- `no_impersonation`：不用于仿冒或对外误导；
- `original_audio_only`：本次不生成新语音；
- `decision`、`used_at`、`created_at`。

`actor_label` 在真实资料模式下加密。授权一次性使用；故事顺序或清单变化后必须重新授权。

### 4.2 `keepsakes`

- 归属与版本：`elder_id`、`authorization_id`、`version`、`title`、`story_manifest`；
- 任务：`status`、`progress`、`attempt`、`error_code`、`idempotency_key`；
- 产物：`relative_path`、`mime_type`、`duration_ms`、`width`、`height`、`size_bytes`、`sha256`；
- 加密：`encryption_version`、`plaintext_size_bytes`、`plaintext_sha256`；
- 透明说明：`renderer`、`cost_cents=0`、`source_mode=original_audio_only`。

`title` 在真实资料模式下加密；`story_manifest` 只保存非原文的故事 ID、顺序、音频/照片资产 ID 和哈希。

### 4.3 状态机

```text
queued → rendering → ready
   └→ failed_retryable → queued（最多 3 次）
                         └→ failed_final
ready → corrupt（下载或播放前完整性失败）
```

服务启动时将遗留的 `queued` / `rendering` 标记为 `failed_retryable + PROCESS_INTERRUPTED`，由用户明确重试，不重复暗中生成。

## 五、主链路与视频规则

1. 读取已确认故事，只标记有 `audio_original` 的故事为可选。
2. 用户选择 1～10 篇，后端重新核对故事归属、归档状态和音频完整性。
3. 后端生成清单 SHA-256，用户勾选四项授权，保存一次性授权记录。
4. 创建幂等任务，立即返回任务状态，后台开始合成。
5. 每篇故事使用其原始录音；有关联图片时显示原图，否则显示本地生成的故事卡。
6. 故事标题、人生阶段、已人工确认和原始录音标识从数据库读取，不调用模型生成文案。
7. 音频先转为稳定 WAV，每段编码为同规格视频，再无损拼接并写入 MP4 `faststart` 元数据。
8. 真实资料模式下，所有解密中间文件只位于 `0600` 临时目录，任务结束无论成功或失败均清理。
9. 输出进入受控资产目录，加密家庭立即加密，然后保存元数据并标记 `ready`。

## 六、API 设计

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/api/v1/elder-profiles/{id}/keepsake-catalog` | 读取可选故事、是否有原声/照片与不可选原因 |
| POST | `/api/v1/elder-profiles/{id}/keepsake-authorizations` | 核对清单并保存一次性授权 |
| GET | `/api/v1/elder-profiles/{id}/keepsakes` | 按版本倒序读取念想任务/产物 |
| POST | `/api/v1/elder-profiles/{id}/keepsakes` | 使用未消耗授权创建幂等合成任务 |
| GET | `/api/v1/keepsakes/{id}` | 查询进度、成本、错误和结果 |
| POST | `/api/v1/keepsakes/{id}/retry` | 对可重试任务增加 attempt 并再次合成 |
| GET | `/api/v1/keepsakes/{id}/content` | 完整性验证后播放或下载 MP4 |

通用错误包结构保持不变。新增核心错误码：`NO_ELIGIBLE_STORIES`、`STORY_NOT_CONFIRMED`、`SOURCE_AUDIO_MISSING`、`SOURCE_ASSET_CORRUPT`、`KEEPSAKE_AUTHORIZATION_REQUIRED`、`KEEPSAKE_AUTHORIZATION_MISMATCH`、`KEEPSAKE_RENDER_FAILED`、`KEEPSAKE_NOT_READY`、`KEEPSAKE_CORRUPT`。

## 七、正式 Web 纵向切片

- 新增 `/keepsake`，从“回忆档案”的明确按钮进入；不增加第五个底部导航，避免手机首屏再次变乱。
- 首屏先显示本地、原声、0 元、不生成虚构语音四项边界，再显示可选故事。
- 未生成时一次只突出“选故事并确认授权”；生成后一次只突出“预览/下载”。
- 生成进度以后端为准，刷新页面后通过念想 ID 恢复。
- 失败显示非技术文案和“重试本机合成”，不要求用户理解 FFmpeg。
- 手机宽度下故事单列、授权逐项显示、视频宽度 100%，并继续保留底部导航安全区。

## 八、测试要求

### 8.1 数据库和后端

- Alembic `upgrade → downgrade → upgrade`，旧数据库不丢档案、故事和媒体。
- 跨家庭故事、未归档故事、无原声故事、重复故事 ID 被拒绝。
- 授权清单哈希绑定、一次消耗、修改清单失效、未全选四项确认被拒绝。
- 同一幂等键不产生第二个任务或版本。
- 中断任务恢复、最多三次重试、失败不删除来源资产。
- 完整性损坏、路径穿越、错误密钥、临时文件清理和加密往返。

### 8.2 真实本地媒体冒烟

- 用隔离数据库、市场空间生成的 WAV 和本地测试图片，跑通“归档故事 → 授权 → 真实 FFmpeg → MP4”。
- `ffprobe` 或 FFmpeg 验证容器、H.264、AAC、720p、时长、音轨与 `faststart`。
- 检查视频中不存在语音合成、人脸驱动或网络请求。

### 8.3 前端和端到端

- Vitest / RTL：状态文案、授权禁用、任务轮询、失败重试。
- Playwright：桌面与 390 px 完成选故事、四项授权、提交、进度、刷新恢复、预览/下载。
- 检查无横向溢出、视频不被底部导航遮挡、表单 Label、焦点和状态播报。

## 九、非技术验收清单

- [ ] 我能看懂这次使用的是老人原始录音，念念没有新编一段老人没说过的话。
- [ ] 我能看懂素材不会离开这台 Mac，本次没有新增付费。
- [ ] 我只能选择已确认并有原声的故事。
- [ ] 我不勾选完四项授权时不能生成。
- [ ] 我能看到真实生成进度，刷新后不会丢失。
- [ ] 生成失败时我能重试，原故事、录音和照片不会被删除。
- [ ] 我能在电脑和手机上播放视频，并下载一份 MP4 到本地。
- [ ] 视频上明确显示“家庭记忆整理 · 原始录音”，不会被误认为真实当下影像。
- [ ] 真实资料模式下，视频重启后仍可播放，备份与恢复规则没有被破坏。

## 十、风险与回退

- 原始录音可能音量不一：本阶段只做格式标准化和安全音量归一，不修复或伪造声线。
- 长音频合成慢：限制 10 篇 / 30 分钟，后台任务与进度可恢复；不在请求中同步等待。
- H.264 编码器或字体缺失：开发时做启动前自检，失败返回可理解的错误，不覆盖旧版本。
- 不同浏览器对 MP4 编码支持差异：固定 H.264 `yuv420p` + AAC + faststart，用 Chrome 和真实 iPhone Safari 验收。
- 第四阶段分支可独立回退到 `第三阶段分支`，不改写前三阶段历史。

## 十一、交接给第五阶段

第四阶段结束后，第五阶段可复用念想版本、媒体清单、文件哈希、本机加密、备份和下载能力，再单独设计账号、指定后代、对象存储、分享授权、撤销和继承规则。
