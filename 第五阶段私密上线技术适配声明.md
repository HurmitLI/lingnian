# 聆年 · 第五阶段 5B 私密上线技术适配声明

> 分支：`第五阶段私密上线分支`
>
> 目标：把已验收的 V0.9 本机版改造成可通过 HTTPS 访问、受邀请登录、数据可恢复的私密内测版
>
> 当前状态：完成只读盘点与本地安全准备；等待用户确认上线等级、资料上云范围和预算后进入架构实现

## 1. 结论

当前代码可以作为上线改造的业务基础，但不能原样部署后承载真实家庭资料。页面能打开只代表“程序被放到云上”，不代表登录隔离、加密密钥、数据库、录音、照片和视频已经安全可靠。

5B 必须把“能访问”与“能安全保存真实家庭资料”分开验收：快速演示版只允许虚构数据；正式私密内测版必须先完成身份、权限、持久化、密钥、备份恢复和监控。

## 2. 已确认的现状

- 前端是 Next.js 16、React 19 和 Node 22～24；后端是 FastAPI、SQLAlchemy、SQLite 和 Python 3.11。
- 前后端本地通过同源代理访问，正式 API 没有登录身份和家庭级授权校验。
- `/elder-profiles` 等接口会读取全部档案，当前只因服务绑定 `127.0.0.1` 才没有公网越权风险。
- 业务数据使用 SQLite；录音、照片、回忆录和视频使用本机文件目录。
- 家庭主密钥保存在当前 Mac 钥匙串；云端 Linux 实例无法使用这套密钥存储。
- 转写、故事整理和视频合成使用进程内后台任务；实例重启时只能标记为可重试，尚无云端任务队列。
- 本地 FunASR 和 FFmpeg 依赖模型缓存、较长运行时间和本机磁盘，不适合不加改造地运行在短生命周期函数实例中。

## 3. veFaaS 适配结果

- 本机 `vefaas` CLI 为 0.3.1，已经是当前可用最新版。
- 后端 `vefaas inspect` 未识别 FastAPI，并给出 Python 3.12、带 `--reload` 的启动命令；这与项目 Python 3.11 边界和生产要求不符，部署时必须显式设置兼容运行时与无热重载启动命令。
- 前端识别为 Next.js，但给出 Node 20；项目声明 Node 22～24，必须在部署前解决运行时兼容并重新构建验证。
- 前后端应作为两个 Application 部署；前端入口通过同源代理访问后端，避免把后端地址和跨域凭据暴露给浏览器。
- 已为前后端增加 `.vefaasignore`，阻止 `.env`、虚拟环境、模型缓存、本机数据、测试产物和账号绑定配置进入部署包。

## 4. 两种上线等级

### 快速演示版

- 只使用虚构数据，验证公网 HTTPS、页面、导航和基本请求。
- 可以暂时使用云实例临时目录，不迁移真实录音、照片、故事和密钥。
- 不承诺长期保存、跨家庭隔离和灾难恢复，不供家人真实使用。

### 正式私密内测版（推荐目标）

- 仅受邀成员登录；所有接口从登录身份推导家庭范围，不能信任浏览器传来的 `family_id`。
- 业务数据进入可靠持久化数据库；音频、照片、PDF 和视频进入私有对象存储。
- 每个家庭使用独立数据密钥；云端只保存经过主密钥或密钥服务封装的密钥材料。
- 上传、播放和下载必须经过权限校验，不能使用公开对象地址。
- 长任务必须可恢复、可重试且不依赖某一个 Web 实例持续存活。
- 备份必须能够在空环境恢复，并验证数据库、密钥材料和全部媒体完整性。

## 5. 必须由用户选择的隐私模式

本机版的核心边界是“原始录音不离开 Mac”。公网版如果要让手机在任何地方录音并处理，必须从以下模式中选择：

1. **全云私密模式**：录音加密上传云端并在云端处理。使用最方便，但原始录音会离开 Mac。
2. **本机配对模式**：云端只负责登录和同步，加密录音回到一台常开的家庭 Mac 转写、整理和合成。隐私边界最接近当前版本，但开发和运维复杂度最高。
3. **只同步成果模式**：录音、转写和视频继续在 Mac 完成，云端只保存家人明确选择的已确认故事或成品。最克制，但手机在外不能完成全流程。

未确认该模式前，不迁移真实家庭资料，也不改写现有 Mac 钥匙串和恢复包。

## 6. 技术取舍

- 保留 FastAPI、Next.js、SQLAlchemy、Alembic 和现有 API 业务语义，不为上线重写核心产品。
- 新增身份、授权、存储、密钥和任务抽象；本机实现继续可用，云端实现通过配置切换。
- 正式私密版不把 `/tmp` SQLite 加定时备份当成真实家庭资料的最终数据库；如果账号条件不支持合适的云数据库，先停留在虚构数据演示版或改选持久化方案。
- 对象存储保持私有；媒体读取通过后端鉴权流式返回或短时签名地址完成。
- 日志只记录 `trace_id`、状态、耗时和非敏感资源 ID，不记录姓名、故事正文、录音内容、令牌、密钥和恢复口令。
- 声音克隆、照片驱动口型和第三方图生视频不进入 5B。

## 7. 进入开发的门槛

开始身份和云存储实现前，必须确认：上线等级、第一批登录人员、三种隐私模式之一、允许上云的资料类型、删除与保留规则、云账号认证类型和月度费用上限。
