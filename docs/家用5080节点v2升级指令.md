# 家用 RTX 5080 节点 v2 升级指令

把下面整段发给家里 Windows 电脑上的 Codex。不要把节点密钥复制进聊天；让那台电脑只在本机迁移已有密钥。

```text
请升级聆年家用生成节点到 v2，并持续执行到 doctor 全部通过。代码仓库是 https://github.com/HurmitLI/lingnian.git，目标分支是“聆年第十六阶段家用影片执行器分支”，执行器目录是 generation-worker。

先只读检查当前 0.2.0 节点的进程、启动项、本机配置、ComfyUI 地址、FFmpeg/FFprobe、Python 版本、RTX 5080 和现有 API 格式工作流。不要输出、截图或提交节点密钥、家庭素材、Cookie 或账号密码。不要开放 8188 公网端口。

确认当前没有正在生成的任务后停止旧节点，但保留可恢复备份。拉取目标分支，使用 generation-worker/scripts/install_windows.ps1 安装 v2。优先在本机把旧节点密钥迁移到 Windows 凭据管理器；如果确实无法读取旧密钥，再停下来告诉我需要在正式 /admin 生成新的一次性连接密钥，不要自行创建多个节点。

检查现有 ComfyUI 工作流和模型，从可运行的工作流复制一份纪实空镜 API JSON，按照 generation-worker/workflows/README.md 配置占位符。每个镜头最多生成 5 秒，目标是适配 16GB RTX 5080；不要一次生成 45–90 秒。工作流必须输出执行器可读取的 MP4、WebM、PNG、JPEG 或 WebP。

运行 lingnian-worker doctor。只有云端、系统凭据、RTX 5080、ComfyUI、API 工作流、FFmpeg 和 FFprobe全部通过后，才启用登录自启任务 LingnianGenerationWorker。然后确认正式后台的在线节点程序版本变为 lingnian-worker/2.0.0。

不要自动提交真实家庭素材或新的 GPU 生成任务。完成后只汇报：v2 是否在线、工作流文件路径、ComfyUI 是否仅监听本机、FFmpeg/FFprobe 是否通过、可用能力、仍需我处理的唯一事项。不要回显任何密钥。
```
