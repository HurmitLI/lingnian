# 家用 RTX 5080 节点 v2.0.2 复验升级指令

把下面整段发给家里 Windows 电脑上的 Codex。不要把节点密钥复制进聊天；让那台电脑只在本机迁移已有密钥。

```text
请把聆年家用生成节点从 lingnian-worker/2.0.1 升级到 2.0.2，并持续执行到 doctor 全部通过。代码仓库是 https://github.com/HurmitLI/lingnian.git，目标分支是“聆年第十六阶段家用影片执行器分支”，执行器目录是 generation-worker。

先只读检查当前 2.0.1 节点的进程、启动项、本机配置、ComfyUI 地址、FFmpeg/FFprobe、Python 版本、RTX 5080 和现有 API 格式工作流。不要输出、截图或提交节点密钥、家庭素材、Cookie 或账号密码。不要开放 8188 公网端口。

确认当前没有正在生成的任务后停止旧节点，但保留可恢复备份。拉取目标分支并确认最新提交包含 2.0.2，使用 generation-worker/scripts/install_windows.ps1 原位升级。必须复用 Windows 凭据管理器中的现有节点密钥；不要生成新节点，不要覆盖家庭素材。

检查现有 ComfyUI 工作流和模型。2.0.2 会自动沿常见 KSampler 的正向/负向条件连接注入逐镜提示和独立随机种子，也兼容 generation-worker/workflows/README.md 的显式占位符；并会把跨重启查重证据与动态镜头数量回传云端。运行 doctor 后，用本地虚构提示各试一个“1982年蓝布包近景”和“1982年中国火车站台”动态镜头，确认两段结果不同、确实包含画面运动且没有现代城市天际线。每个镜头最多生成 5 秒，目标是适配 16GB RTX 5080；不要一次生成 45–90 秒。正式纪实影片工作流必须至少输出一个 MP4、WebM 或 GIF 动态空镜；只输出 PNG、JPEG 或 WebP 的纯静态工作流会被云端质量门禁拒绝。

运行 lingnian-worker doctor。只有云端、系统凭据、RTX 5080、ComfyUI、动态提示绑定、API 工作流、FFmpeg 和 FFprobe 全部通过后，才重新启用登录自启任务 LingnianGenerationWorker。然后确认正式后台的在线节点程序版本变为 lingnian-worker/2.0.2，并且“需升级”提示消失。

不要自动提交真实家庭素材。可以在本机用虚构提示验证工作流，但不要替我在正式云端登记新的家庭成片任务。完成后只汇报：2.0.2 是否在线、工作流文件路径、ComfyUI 是否仅监听本机、两段虚构动态镜头是否不同、FFmpeg/FFprobe 是否通过、可用能力、仍需我处理的唯一事项。不要回显任何密钥。
```
