# 第二阶段验收记录

> 状态：验收通过，第二阶段已完成

> 开始日期：2026-08-29  
> 验收日期：2026-08-29

> 分支：`第二阶段分支`

## 已确认产品决定

- 真实家庭录音只在本机使用 FunASR 转写；真实人工校对稿默认不发送千问，单次明确授权后才允许发送最少必要文字。
- 家庭档案主密钥保存在当前用户的 Mac 钥匙串；家庭另行保管受恢复口令保护的离线恢复包；系统不保留后门。
- 第二阶段只实现应用内本机提醒，不接微信、短信或其他对外发送渠道。

## 当前自动化验证

- 后端测试：33 项全部通过；包含密码学、恢复包、安全 API、敏感文本与流式媒体加解密、真实资料端到端流程、SQLite/资产备份与离线恢复演练、媒体完整性、照片/老物件触发、云模型单次授权门、家庭关系、题库偏好、长期记忆、本机提醒和回忆录测试。
- AES-256-GCM：文字往返、错误密钥、上下文串换和密文篡改检测通过。
- 恢复包：scrypt 派生、错误口令、元数据篡改、文件权限 `0600` 和禁止静默覆盖通过。
- Mac 钥匙串：已识别本机原生 `keyring.backends.macOS.Keyring`；尚未在无用户操作的情况下写入正式主密钥。
- 数据库迁移：`dbf1b18097c8` 已在第一阶段数据库副本上通过 `upgrade → downgrade → upgrade`，旧家庭和媒体分别得到安全默认值 `test` 与 `encryption_version=0`。
- 迁移前备份：已生成忽略于 Git 的 `.runtime/第一阶段数据库备份-第二阶段迁移前.db`，随后实际本机数据库成功升级至 `dbf1b18097c8 (head)`。
- 安全 API：家庭安全状态、显式初始化和恢复包导出接口已实现；现有测试家庭保持 `not_initialized`，没有自动生成正式主密钥。
- 媒体完整性：读取前校验文件大小和 SHA-256；篡改文件会标记为 `corrupt` 并阻止播放。
- 云模型数据边界：真实家庭的问题从本地题库生成；未授权校对稿被后端拒绝；一次性授权与家庭、会话、用途、数据级别和校对稿 SHA-256 绑定，使用后不能复用，修改校对稿会撤销尚未使用的旧授权。
- 真实资料入口：未完成加密迁移前，API 拒绝新建 `authorized_non_sensitive` 或 `authorized_sensitive` 家庭，不能靠绕过前端提前开启。
- 授权任务迁移：`2cb78dd8f82a` 已在数据库副本上通过 `upgrade → downgrade → upgrade`，实际本机数据库已升级至 `2cb78dd8f82a (head)`。
- 前端：安全状态、显式钥匙串初始化、恢复包下载和真实资料单次外发授权界面已实现；lint、TypeScript 和生产构建通过。
- 最终服务验收：前端 `http://127.0.0.1:3011` 与后端健康接口均返回 200，数据库处于 `a9d53e7f4b21 (head)`；桌面页面完整显示，390 px 下页面宽度为 390 px、无横向溢出，浏览器控制台无警告或报错；验收过程未点击正式钥匙串初始化。
- 家庭与长期记忆：已建立人物关系、题库、话题偏好和确认事实数据表；重复关系幂等，跨家庭/自关联会被拒绝；只有人工确认故事进入长期记忆。
- 家庭成员维护：页面可新增、改名和安全删除普通成员，可建立或删除父母/子女/配偶/兄弟姐妹/祖孙/自定义关系；讲述者因关联故事与媒体，后端拒绝从家谱界面直接删除。
- 家谱界面浏览器验收：桌面端已看到家庭成员、改名、角色、添加成员和已确认关系空状态；390 px 下家谱内容宽 `342 px`、单列布局、页面 `scrollWidth=390`，控制台无警告或报错。验收未向当前用户测试档案写入新成员。
- 本地题库：七个人生阶段各三道内置问题，按讲述者的使用次数轮换；问题生成不再需要把讲述者姓名或阶段信息发给千问。
- 讲述者意愿：“不要再问”由后端强制阻断；“先问我是否愿意”缺少本次确认时由后端拒绝；页面可直接设置并看到阶段覆盖度。
- 家庭记忆迁移：`8ae39cd14f6e` 已在数据库副本上通过 `upgrade → downgrade → upgrade`，四张新增表存在，实际本机数据库已升级至 `8ae39cd14f6e (head)`。
- 本机提醒：支持幂等创建、到期读取、只显示一次和人工关闭；不接任何外部渠道；话题改为“不要再问”后，未触发提醒自动暂停且不能新建同话题提醒。
- 提醒重启补偿：到期提醒在模拟服务重启后仍只返回同一条记录，点击显示后 `show_count` 固定为 1，不因重启或重复请求产生二次提醒。
- 文字回忆录：支持从人工确认故事生成不可覆盖的版本化 Markdown，保留故事、阶段、草稿和确认时间清单；下载前重新计算 SHA-256，内容被篡改时阻止下载。
- 提醒与回忆录迁移：`c61e09b4a2d7` 已在数据库副本上通过 `upgrade → downgrade → upgrade`，实际本机数据库已升级至 `c61e09b4a2d7 (head)`。
- 提醒与回忆录浏览器验收：桌面端可见“本机提醒与文字回忆录”、不接外部渠道说明和版本生成按钮；390 px 下卡片宽度 `374 px`、内部单列 `342 px`、页面无横向溢出；控制台无报错。验收未创建实际提醒或回忆录版本。
- 打印版 PDF：使用本机 CJK 字体生成 A4 版式，包含封面、人生阶段、故事、来源追溯和页码；PDF 生成失败不会影响 Markdown。自动化验证页数、标题元数据、中文文本提取、哈希篡改阻断；另以 PDFium 渲染两页 PNG 完成人工视觉检查，未发现中文缺字、截断、重叠或黑块，QA 文件随后删除。本机没有 Homebrew/Poppler，未使用 `pdftoppm`。
- PDF 元数据迁移：`d723bf91e50c` 已在数据库副本上通过 `upgrade → downgrade → upgrade`，实际本机数据库已升级至 `d723bf91e50c (head)`。
- 照片/老物件触发：上传前校验 MIME、真实图片格式、像素尺寸、25 MB 上限与 SHA-256；图片、老物件分别使用固定的不推断问题，模型推断字段保持为空；“不要再问”与“先问我”仍由后端强制执行。
- 图片删除与链路隔离：未归档的触发图片可明确删除，数据库关联随之清除；转写任务和时间线仅选择 `audio_original`，不会把图片误当音频。
- 照片关联迁移：`e84a12c9b630` 已在当前数据库副本上通过 `upgrade → downgrade → upgrade`，并确认 `media_links` 表存在；实际本机数据库已升级至 `e84a12c9b630 (head)`。
- 照片入口浏览器验收：桌面端已看到触发类型、JPEG/PNG/WebP 选择器、家人明确标注和安全说明；390 px 下页面 `scrollWidth=390`、卡片宽 `374 px`、表单宽 `342 px`，无横向溢出；控制台无报错。验收未上传用户图片。
- 真实资料加密门：只有钥匙串主密钥存在、恢复包已下载且用原口令成功回读后，后端才允许把家庭分级改为 `authorized_non_sensitive` 或 `authorized_sensitive`。
- 敏感文本迁移：姓名、出生年、籍贯、转写、草稿、故事、时间表达、长期记忆、图片标注、回忆录与审计人标识进入绑定家庭/对象/字段/密钥版本上下文的 AES-256-GCM 密文，原表只留非敏感占位符。
- 媒体静态加密：原始音频、图片、派生媒体和 PDF 使用流式 AES-256-GCM，密文和解密后明文分别校验尺寸/SHA-256；转写、播放和下载仅创建 `0600` 临时文件并在任务或响应结束后清理。
- 加密端到端验证：在内存密钥库中完成建档、音频迁移、解密播放、本地转写、人工校对、故事整理、确认归档、时间线、Markdown/PDF 回忆录与启用后新建档案；原始 SQLite dump 不包含测试姓名、地点、文本、文件名和确认人明文。
- 加密清除验证：真实资料模式下跳过未归档会话会同步清除音频、转写、草稿及对应密文字段；删除未归档触发图片会同步清除图片标注密文，不留下孤立加密记录。
- 加密存储迁移：`f1b76c3a802e` 已在数据库副本上通过 `upgrade → downgrade → upgrade`，并确认 `encrypted_fields` 表存在；实际本机数据库已升级至 `f1b76c3a802e (head)`。
- 安全界面浏览器验收：页面显示“当前状态 / 恢复包 / 恢复验证”三个状态；390 px 下页面 `scrollWidth=390`、安全卡片宽 `374 px`、状态单列 `342 px`，控制台无警告或报错；未点击正式钥匙串初始化。
- 备份格式：ZIP 中包含 SQLite 在线快照、Alembic 版本、资产路径/尺寸/SHA-256 清单和明确的“不包含主密钥、恢复口令、恢复包”安全声明；路径穿越、非空恢复目录、数据库或资产哈希不匹配均会被拒绝。
- 备份恢复自动化：普通备份已通过“生成 → 新目录展开 → `PRAGMA integrity_check` → Alembic 版本 → 全资产哈希”；加密家庭进一步通过“恢复包 + 口令 → 恢主密钥 → 全部密文字段认证解密 → 全部加密媒体明文哈希”。
- 本机实际备份：在当前仅含测试资料的档案上生成 1 份忽略于 Git 的备份，共含 6 个资产；已恢复到新临时目录并通过数据库与资产验证，随后临时目录自动清理。
- 真实模型冒烟：仅使用明确标注为虚构、不含个人信息的中文文本调用当前配置的 `qwen / qwen3.7-plus-2026-05-26`，成功返回标题、正文和 `source_coverage=1.0`；未输出或记录 API Key。
- 备份清单迁移：`a9d53e7f4b21` 已在数据库副本上通过 `upgrade → downgrade → upgrade`，并确认 `backup_manifests` 表存在；实际本机数据库已升级至 `a9d53e7f4b21 (head)`。

## 最终人工验收

- 用户本人已完成 Mac 钥匙串主密钥初始化、恢复包下载、恢复口令回读验证和真实资料加密启用；系统状态为 `active_encrypted`。
- 用户本人已完成本机备份完整恢复演练；最新备份状态为 `recovery_verified`，已在全新目录恢复并验证数据库，成功认证解密 49 个敏感字段和 4 个加密媒体，Alembic 版本为 `a9d53e7f4b21`。
- 恢复口令、主密钥和恢复包内容未写入源码、数据库、日志、验收文档或 Git。
- 用户于 2026-08-29 明确确认“验证通过”，第二阶段正式验收通过。

## 里程碑状态

- 安全与恢复底座：已完成密码学、钥匙串抽象、恢复包回读门、敏感文本/媒体/PDF 加密迁移、启用后持续加密、云模型单次授权门、密文/明文双重完整性校验、备份清单和新目录恢复演练
- 家谱、题库、敏感话题与跨会话记忆：数据模型、API、本地题库轮换、话题偏好门、确认故事索引与家庭成员/关系新增、改名、删除界面已完成
- 照片/老物件触发：图片上传、预览、关联、删除、完整性校验、不推断问题、数据库迁移和桌面/390 px 浏览器验收已完成
- 本机提醒：后端规则、页面界面、幂等显示与服务重启补偿专项验收已完成
- 文字回忆录与导出：版本化 Markdown、A4 打印版 PDF、来源清单、哈希和下载界面已完成
- 备份恢复和人工验收：备份、新目录展开、数据库与资产校验、正式恢复包解密演练和用户最终人工验收均已完成

## 验收边界

- 真实资料模式已经显式启用；录音仍只允许本机转写，向云模型发送人工校对文本仍必须逐次明确授权。
- `.env`、数据库、媒体、主密钥、恢复口令和恢复包不得进入 Git。
- 最终人工验收步骤见 [第二阶段人工验收清单](第二阶段人工验收清单.md)。
