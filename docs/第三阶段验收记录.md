# 第三阶段验收记录

> 状态：真机核心流程已通过；根据用户反馈完成手机界面减法，等待第二次视觉易用性确认

> 日期：2026-08-29
>
> 分支：`第三阶段分支`
>
> 范围：正式响应式 Web，不含微信、小程序、原生 App 和公网部署

## 已完成

- 建立 `/`、`/record`、`/archive`、`/family` 四个正式页面，以及桌面侧栏和手机底部导航。
- 首页可显示当前讲述者、继续未完成记录、阶段覆盖、到期提醒和最近故事。
- 记录页保留第二阶段完整闭环，并增加录音格式探测、权限分类提示、计时、试听、移除、上传和麦克风中断提示。
- 后台任务以后端为事实来源；页面隐藏时暂停查询，间隔逐步退避，刷新后可由 URL 和最近会话恢复。
- 家庭管理整合讲述者、人物关系、七类话题意愿、加密、备份和恢复状态。
- API 默认使用同源 `/api/v1` 代理，日常浏览器不再依赖写死的 `127.0.0.1:8011`，为手机局域网验收做好准备。
- 增加全局加载页、异常恢复页、网络/超时错误文案、唯一页面标题和未知后端状态兜底。
- 核心正文、48 px 操作目标、显式 Label、焦点轮廓、`aria-current`、`aria-live`、减少动画和颜色对比基础规则已实现。
- 真机首轮验收后做了手机界面减法：记录页一次只突出一个开始动作，照片入口默认收起；档案页先显示故事，提醒和导出收起；家庭管理去掉重复的讲述者选择，七类话题与备份恢复默认收起。
- 底部导航下方预留足够滚动空间，交互控件增加滚动安全区，避免滑到页底后被导航遮挡。

## 自动化结果

- 后端：`34 passed`；新增最近会话读取接口覆盖排序、limit、空状态和不存在讲述者。
- 前端单元/组件：5 个测试文件、13 项测试全部通过。
- 前端静态检查：ESLint、TypeScript strict 和 Next.js 生产构建全部通过。
- Playwright：桌面 Chrome 与 390 px 手机 Chrome 共 10 项通过、4 项按端别设计跳过；新增手机记录页主动作、档案页信息顺序、家庭管理低频设置收起验收。
- Playwright 真实闭环使用临时 SQLite、临时媒体目录和 mock 模型，桌面与手机均完成“建档 → 上传 WAV → 转写 → 校对 → 整理 → 确认 → 时间线”。
- 响应式覆盖 390、768、1280、1440 px，无意外横向溢出；核心按钮最小高度 48 px。
- axe 自动扫描未发现无障碍规则违规。

## 真实 iPhone 验收结果

- Safari 通过同一 Wi-Fi 的临时本地 HTTPS 进入念念首页。
- 麦克风授权、开始录音、计时、停止录音和试听均正常。
- 录音确认上传成功，真实本地 FunASR 任务成功。
- 用户选择“这次不想讲”后，后端已确认会话状态为 `SKIPPED`，媒体、转写、草稿和任务记录均已清理。
- 用户删除临时 CA 后确认“手机端验收通过”。
- 用户同时指出首轮界面入口过多、布局显乱，因此进入了第二轮手机界面精简，未将第三阶段直接判定为最终完成。

## 人工浏览器检查

- 本机正式服务首页、开始记录、回忆档案和家庭管理均返回 200。
- 家庭管理真实页面正确显示“讲述者档案 / 讲述意愿 / 家庭档案安全”，七个话题设置与备份恢复可展开、可再次收起。
- 当前加密状态可读取为“本机加密已完成”；检查过程没有修改真实资料、话题偏好或备份。
- 桌面与 390 px 页面无横向溢出，导航当前态和页面标题正确，页面未显示错误。
- 390 × 844 真实数据界面复查：记录页高度由约 2886 px 降到 864 px，家庭管理由约 3046 px 降到 1413 px，档案页低频工具默认不显示表单。
- 对展开后的档案工具、话题意愿、本机备份进行真实点击检查，均可正常展开；当前可见操作控件逐个滚动到可见区后，未发现被底部导航挡住的点击目标。

## 唯一待完成项

不需要重做录音、上传和清理闭环。用户只需在真实手机 Safari 上第二次查看四个页面，确认：

1. 首页和记录页的主动作是否一眼能看懂；
2. 回忆档案是否先看到故事，不再被提醒和导出表单干扰；
3. 家庭管理的话题意愿和备份恢复是否默认收起；
4. 底部导航是否还会挡住想点的内容。

这一次真机视觉确认通过后，更新本记录为“验收通过”，提交最终验记录并封存第三阶段中文分支。
