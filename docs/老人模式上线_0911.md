# 老人模式前端发布（2026-09-11）

本次发布保留普通模式，新增老人模式：大字号、大按钮、三个主要导航入口；首页突出开始讲述和回听；录音页按需展开讲述人、历史回答及管理操作。选择保存在当前浏览器，刷新后保留，可随时切回普通模式。

## 火山部署

- 正式站点：https://snj9mfcngm6tv8vlfvj2c.apigateway-cn-beijing.volceapi.com/
- 前端函数：`lingnian-formal-web`（`mjx3aylx`）。
- 稳定版本：40 → 41，流量已切至 41。
- 本轮只发布前端；继续连接既有正式后端，不改变后端、生成节点或家庭数据。
- 构建启用 `NEXT_PUBLIC_FORMAL_AUTH_REQUIRED=true` 和 `FORMAL_AUTH_REQUIRED=true`，API 代理使用既有正式 HTTPS 地址。
- 运行包没有 `.env` 文件；凭据继续由云端配置提供。

## 验证

- ESLint、TypeScript 检查通过。
- 单元测试：89 项通过。
- 浏览器测试：32 项通过、4 项按设备条件跳过。
- 正式站点已有登录会话下检查：普通模式进入老人模式成功；首页视觉检查通过；刷新保留；开始聊聊进入简化录音页。
- 本次线上检查未新建录音、修改故事或提交生成任务。功能检查不代替真实老人可用性测试。

## GitHub 范围

`codex/elder-mode-release-0911` 是前端发布分支，基于已公开的第十六阶段分支，补齐当前前端依赖并同步本次界面。保留原默认分支。

此分支只同步前端源码、测试和发布说明；不包含本地尚未公开的历史提交、录音、视频样片、运行数据或环境凭据。后端目录保留基线版本；验证当前线上功能时使用上述既有正式后端，本次不宣称同步了后端最新源码。

本地开发参照 `frontend/.env.production.example` 配置自己的服务地址；安装依赖后运行 `npm run dev`。生产构建运行 `npm run build:vefaas`，构建产物为 `.next/standalone`。
