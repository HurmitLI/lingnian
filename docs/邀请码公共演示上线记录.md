# 邀请码公共演示上线记录

> 日期：2026-08-30
>
> 分支：`邀请码公共演示分支`
>
> 当前状态：公网发布完成；本地与公网自动验收通过

## 上线形态

本次只发布一个前端只读演示应用，不发布后端、SQLite、本机密钥、模型 Key 或真实家庭档案。受邀人输入邀请码后，可以看到虚构人物“沈素琴”的完整样片：家庭影像、故事文字、照片和产品处理流程。

公网演示和本机私密完整版互相隔离：

- 演示版没有录音、上传、家庭管理、数据库和模型调用入口。
- 公网包内只有虚构样片素材，不包含根目录 `.env` 和 `data/` 内容。
- 本机完整版仍只绑定 `127.0.0.1`，不因本次发布而改变。

## 邀请码与会话安全

- 邀请码只在服务端校验，使用 SHA-256 等长比较，不进入浏览器代码。
- 登录成功后签发 12 小时有效的 HMAC-SHA256 会话，Cookie 使用 `HttpOnly`、`SameSite=Strict`，公网 HTTPS 下同时使用 `Secure`。
- 登录接口校验同源请求，单个运行实例内限制连续错误尝试。邀请码使用高熕随机值，不依赖简单短口令。
- 未登录时，`/showcase` 跳转登录页；照片和视频直链返回 401。退出后 Cookie 立即失效。
- 邀请码和会话密钥不进入 Git、前端公开变量、部署包和上线记录。

## 云端配置

已使用现有北京区域的 `AI-gateway`，新建独立应用 `lingnian-invite-demo`：

- 公网地址：<https://skd979vd6hlk1f8c0301j.apigateway-cn-beijing.volceapi.com/>
- 应用 ID：`877b91d9e020`；函数 ID：`5pgps2t9`。

- 框架：Next.js 16.3，静态页 + Node.js 路由处理器。
- 运行时：veFaaS `native-node20/v1`。Next.js 官方最低要 Node 20.9，当前代码不使用 Node 22 专属 API。
- 规格：0.5 vCPU、1 GiB 内存、最小实例 0、最大实例 1，无预留实例。
- 构建：`npm ci && npm run build:vefaas`，将 Next.js standalone 服务、必要依赖、静态资源和样片媒体组成最终运行包。
- 启动：`PORT=3011 HOSTNAME=0.0.0.0 NODE_PATH=./runtime_deps node server.js`。专用的 `runtime_deps` 和 `runtime_next` 目录避免被根目录 `node_modules/` 与 `.next/` 部署排除规则误伤；构建脚本会把生成的 standalone 启动配置精确改为 `runtime_next`，运行时不写代码目录。
- 服务端环境变量：`DEMO_PUBLIC_MODE`、`DEMO_INVITE_CODES`、`DEMO_SESSION_SECRET`。三者都不使用 `NEXT_PUBLIC_` 前缀。

## 费用边界

当前账户 2026 年 8 月已产生约 17.18 元费用，主要来自已有函数服务。新演示应用采用按量计费和零预留实例；无访问时不保持常驻实例。根据火山引擎当前官方按量价格，费用主要由调用次数、执行时间、内存和公网流量构成；视频为 2.5 MB，少量邀请体验的流量可控。

产品经理已于 2026-08-30 明确同意新增按量计费演示应用，新增月费目标不超过 20 元。当前采用零预留、最多 1 个实例、邀请码访问和只读固定素材来压低计算与流量费用。该配置是费用控制措施，不等于云厂商账单的硬性封顶；若后续扩大邀请码投放，需要先检查账单并重新评估流量。

## 本地验收证据

- ESLint：通过，0 个错误、0 个警告。
- TypeScript strict：通过。
- Vitest：9 个测试文件、23 项全部通过。
- Next.js 生产构建：通过，`/showcase` 为服务端动态路由，两个认证接口为动态路由。
- 原有完整版 Playwright 回归：桌面/手机 10 项通过、4 项按设备条件正常跳过。
- 邀请演示 Playwright 专项：桌面/手机 2 项通过，包括错码、正码、Cookie、直链、退出、溢出和无障碍扫描。

## 公网验收证据

- veFaaS 应用状态：`deploy_success`；Node 20 服务启动正常。
- 公网根地址：返回 307 并跳转 `/demo-login`。
- 未登录媒体：视频返回 401；错误邀请码返回 401。
- 正确邀请码：登录返回 200，样片页返回 200；Cookie 使用 `HttpOnly`、`Secure`、`SameSite=Strict`。
- 视频：完整请求返回 200、`video/mp4`、2,612,542 字节；Range 请求返回 206 和准确的 1,024 字节，支持浏览器拖动播放。
- 退出：返回 200；退出后再次访问视频返回 401。
- 发布排障：Next.js standalone 依赖和构建目录改名后打包，静态媒体统一设为运行进程可读；未上传 `.env`、数据库、本机主密钥和真实家庭资料。
- 桌面 Chrome 与 390 × 844 手机 Chrome 的公网浏览器自动验收均已纳入测试，覆盖错码、正码、视频、退出、横向溢出和无障碍扫描。
