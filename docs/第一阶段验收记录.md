# 第一阶段验收记录

> 验收日期：2026-08-29  
> 状态：第一阶段代码、本机纵向切片与通义千问真实 API 冒烟均已完成。  
> 数据限制：本次只使用虚构人物资料与系统合成音频，没有录入真实家庭资料。

## 自动化验证

- 后端 pytest：通过，9 项测试全部通过；存在 1 条 Starlette TestClient 上游弃用提示，不影响应用结果。
- 数据库迁移：通过，已验证 `upgrade → downgrade → upgrade`，当前位于 `b3a1f44d9164 (head)`。
- 前端 ESLint：通过。
- 前端 TypeScript：通过。
- 前端生产构建：通过，Next.js 16.3.3 成功生成 `/` 静态路由。
- Python 依赖：`pip check` 通过；已生成 `backend/uv.lock`，锁定基础、开发和 ASR 可选依赖。

## 真实能力冒烟

- 本地 FunASR：通过。使用系统合成的 16 kHz 单声道 WAV，Paraformer 输出“小时候我常常跟着奶奶去河边那时候的事情我现在还记得”；模型推理约 0.58 秒，整次进程（含模型加载）约 5.57 秒。
- 通义千问云模型：通过。已锁定 `qwen3.7-plus-2026-05-26`（非思考模式、JSON 输出），并分别通过虚构人物的温和提问与忠实故事整理冒烟；整理结果保持原文事实，`source_coverage=1.0`。
- 浏览器完整链路：通过。已验证虚构档案、回忆会话、音频入库、mock 转写、人工校对、故事整理、退回修改、再次整理、人工确认归档、时间线、跳过与刷新持久化。
- 390 px 移动端：通过可视检查，主要内容无可见横向溢出，录音与归档音频控件可见。
- 浏览器文件选择：浏览器自动化扩展拒绝本机路径注入（`Not allowed`）；改由同一上传 API 写入系统合成音频后继续页面验收。文件选择控件已确认能打开，上传接口及上传后 UI 均已分别验证。

## 验收时修复

- 为 Next.js 本机开发配置加入 `allowedDevOrigins: ["127.0.0.1"]`，解决开发资源被跨源保护拦截的问题。
- 建档表单在异步请求前保存表单节点引用，解决请求完成后事件对象上的 `currentTarget` 已为空而导致的 `reset` 报错。
- 停止脚本改用进程工作目录确认归属，并在成功停止后清除 PID 文件。

## 已知边界

- 第一阶段不录入真实老人隐私资料。
- 第一阶段不公开部署、不实现正式登录、加密恢复和权限继承。
- 当前本机 `.env` 已启用 `ASR funasr / LLM qwen`；需要纯离线调试时，可临时切回 mock，但 mock 结果不能代替真实能力验收。
- 当前 Node 24.19.0 在项目声明的 Node 22～24 范围内；ESLint 固定为 9.39.5，以兼容当前 `eslint-config-next`，安装时会看到其生命周期提示。
