# 聆年 · 第五阶段前端技术适配声明

> 适用阶段：第五阶段 5A「V0.9 前端体验重构与发布准备」  
> 目标终端：响应式 Web（Mac/电脑浏览器 + 手机浏览器）  
> 发布定位：家庭私密内测版，不直接公开传播  
> 状态：5A 全站重构、自动化与正式数据只读检查已完成，等待用户最终人工验收

## 1. 项目判断

- 「聆年」的后端核心、静态加密、备份恢复、录音转写、故事归档和本机原声视频已经完成；当前主要矛盾不是“有没有功能”，而是功能是否容易理解、是否像一款完整产品。
- 第三、第四阶段已经有可用前端，不从零重写。第五阶段先做信息架构、交互层级和视觉质量重构，再完成真实前后端总验收，最后进入私密上线。
- V0.9 的核心价值是“把家人亲口讲过的故事安全留下来”，不是用更多按钮展示技术能力。
- 首轮首页与记录页保留为功能 MVP 和交互验证稿，但用户已明确否定其上线质量。后续必须按完整产品而非“润色 MVP”的标准重构。
- 讲述者不得以单一测试数据等同于固定用户；正式产品必须提供同一家庭内的新增、编辑和明确切换。

## 2. 当前能力与版本边界

### V0.9 包含

- 原始录音、试听、上传、本机 ASR 转写、人工校对、忠实整理和人工确认；
- 已确认故事、原声音频、照片/老物件、文字/PDF 回忆录和本机原声视频；
- 任务进度、失败重试、刷新恢复、本机加密、备份和恢复；
- 响应式 Web、老人友好交互、真实浏览器和真机验收；
- 私密上线前的前后端完整联调与发布准备。

### V0.9 不包含

- 声音克隆、自定义音色训练或 TTS 模拟本人说新内容；
- 照片驱动口型、生成式人物动画或第三方图生视频；
- 未经确认的公网开放、公开分享、云素材上传和付费生成平台；
- 仅为“看起来专业”而引入复杂状态库、组件库或大规模重写。

声音克隆和生成式老照片视频进入上线后的独立版本路线，届时必须重新设计本人授权、撤销删除、AI 合成标识、供应商隐私和成本规则。

## 3. 技术选择

- 保留 Next.js 16 App Router、React 19、TypeScript strict、Tailwind CSS 3、Lucide 和当前 npm 锁文件；本阶段不跨大版本升级。
- 保留 FastAPI `/api/v1` 作为业务与权限事实来源；浏览器不读取数据库、Mac 钥匙串或模型密钥。
- 继续使用集中式 API 客户端、URL 恢复状态、局部 React 状态和后端持久化任务；当前不引入 TanStack Query、Zustand 或 Redux。
- 复用现有 App Shell，但新增轻量设计基础组件和业务分区组件，逐步拆分过大的 `workspace-app.tsx`，不进行一次性高风险重写。
- 使用现有轮询处理长任务；后端状态决定排队、运行、等待人工、成功、失败和恢复，前端动画不冒充任务完成。
- 继续使用 Vitest + React Testing Library + Playwright；最终必须对接真实 FastAPI、SQLite 和本机媒体链路。

## 4. 视觉与交互方向

- 视觉关键词：温暖、克制、可信、像家庭记忆册，不像开发后台或功能清单。
- 保留米白、深褐、朱红和柔和绿色，但建立更明确的字体、间距、圆角、阴影、按钮和状态层级。
- 展示标题使用中文衬线字体，操作、表单和状态使用易读的系统无衬线字体；不引入无授权商业字体。
- 首页只突出“继续记录/开始记录”一个主要动作；次要统计和工具退后。
- 记录流程改为清楚的分步骤体验：选择回忆入口 → 录音/照片 → 转写校对 → 故事确认。
- 档案优先展示人物、照片、故事和声音，导出、提醒、安全等低频操作渐进展开。
- 原声视频页面呈现为“念想制作”，先选故事与照片，再授权，最后查看产物；不把安全说明堆成首屏噪声。
- 手机继续保留四个底部入口；每屏一个主要动作，核心触控目标不小于 48px，无意外横向滚动。

## 5. 页面与真实闭环

| 页面 | V0.9 重构重点 | 后端事实来源 |
|---|---|---|
| `/` | 今日主任务、继续未完成记录、最近故事 | 讲述者、会话、时间线、提醒 |
| `/record` | 四步记录流程、清楚的当前状态和下一步 | 会话、媒体、转写、任务、草稿 |
| `/archive` | 故事卡/时间线、原声、照片关联、回忆录入口 | 已确认故事、媒体、回忆录 |
| `/keepsake` | 选故事、授权、真实进度、预览与下载 | 念想目录、授权、任务、MP4 |
| `/family` | 人物与意愿优先，安全和备份作为低频管理 | 人物关系、偏好、安全、备份 |

核心业务闭环：

```text
进入首页 → 继续/开始记录 → 录音或照片触发 → 本机转写
→ 人工校对 → 忠实整理 → 人工确认 → 档案回看
→ 选择故事制作原声视频 → 播放/下载 → 刷新恢复
```

## 6. 状态与安全

- 首次加载、空档案、网络失败、权限不足、等待人工、长任务运行、可重试失败、最终成功和未知状态分别呈现。
- 重要提交立即禁用重复操作，继续使用后端幂等键；刷新后从 URL 和任务 ID 恢复。
- 真实录音只在本机转写；向千问发送人工校对文本仍需单次明确授权。
- API Key、恢复口令、主密钥、原始录音和完整故事不进入前端日志或埋点。
- V0.9 不增加第三方分析、错误监控或媒体平台，避免在隐私方案未确认前产生新的外发路径。

## 7. 验收门槛

- 390、768、1280、1440 px 核心页面可用，无横向溢出和固定导航遮挡。
- 桌面 Chrome 与手机 Chrome 跑通真实隔离闭环；麦克风、播放和下载由真实 iPhone Safari 复验。
- `npm run lint`、`npm run typecheck`、`npm run test`、`npm run build`、`npm run e2e` 全部通过。
- 浏览器 Console 无持续错误；加载、空、失败、等待、成功和恢复状态均有验收证据。
- 不使用 Mock 页面或构建成功代替真实前后端联调。

## 8. 偏离与待确认

- 不升级 Tailwind 4：现有 Tailwind 3 与语义 CSS 变量已稳定，升级不增加本阶段用户价值。
- 不立即引入组件库：当前复杂交互较少，先用语义 HTML 和轻量基础组件减少视觉不一致。
- 不立即正式公网部署：前端与真实后端验收通过后，再读取上线部署手册并确认云账号、域名、费用和私密访问范围。
- 2026-08-30 用户验收结论：首轮 UI 作为 MVP 可理解，但不具备上线可用性；讲述者也缺少可发现的编辑和切换能力。
- 因此不进入 5B 上线，先完成讲述者管理，再对全站做上线级信息架构、状态与视觉系统重构。
- 2026-08-30 已完成讲述者管理和全站 UI 统一：档案、原声视频、家庭管理与代表页使用同一套布局、字体、色彩、按钮、卡片、展开项和响应式规则。
- 桌面与 390 px 真实浏览器检查、前端静态与单元测试、生产构建、正式 Web 检查、桌面/手机隔离核心闭环均已通过；最终是否达到上线视觉质量仍以用户全站验收为准。
- 产品展示品牌已更名为「聆年」，最终候选 Logo 为“窗边暖灯”图形；为保障已有本机档案可恢复，内部 `niannian` 技术标识本阶段不迁移。
