# 聆年 · 前端技术适配声明

> 历史说明：本文件记录第三阶段首次正式 Web 建设时的适配决定。第五阶段 V0.9 前端重构采用的当前声明见 `第五阶段前端技术适配声明.md`。

> 适用阶段：第三阶段「正式前端与目标终端」  
> 目标终端：正式响应式 Web（电脑浏览器 + 手机浏览器）  
> 状态：已确认，可进入开发

## 1. 项目判断

- 「聆年」是隐私优先、长期积累的家庭口述史产品，直接用户包括老人和陪伴记录的子女。
- 前两阶段已经跑通真实后端、录音、本地转写、校对、归档、家谱、回忆录、静态加密和完整恢复；第三阶段不重写业务能力，而是把已有最小验收页面升级为正式 Web 产品。
- 本阶段采用纵向切片扩展：先建立正式信息架构和代表首页，再逐条迁移并验收记录、校对、归档、家庭管理和安全能力。
- 用户已确认唯一主终端为响应式 Web。微信、小程序、桌面原生端不在本阶段并行建设。

## 2. 输入资料

- 产品 PRD：`niannian.md`
- 通用工程规范：`通用技术栈手册.md`
- 前端工程规范：`前端技术栈手册.md`
- 已确认技术边界：`技术适配声明.md`
- 第二阶段交接：`第二阶段开发手册.md`、`docs/第二阶段验收记录.md`
- 现有代码：FastAPI `/api/v1`、Next.js App Router、SQLite、受控本地媒体目录和 Mac 钥匙串安全能力

## 3. 技术选择

- 保留 Next.js 16 App Router、React 19、TypeScript strict、Tailwind CSS 3 和现有 npm 锁文件，不做大版本升级。
- 保留 FastAPI 作为所有业务事实来源；前端只通过集中式 API 客户端访问，不读取数据库或钥匙串。
- 页面按业务功能拆分，交互组件保留在 Client Component 边界内；不为了形式一次性重构全部后端或引入全局状态库。
- 增加 Vitest + React Testing Library，用于状态映射、错误归一化、表单与组件测试；增加 Playwright，用隔离测试数据库跑正式 Web 核心闭环。
- 使用原生 `MediaRecorder` 做回合制录音，运行时探测浏览器支持的 MIME；不在第三阶段引入全双工语音、TTS 或 WebSocket。
- 不引入 TanStack Query、Zustand、完整组件库或图表库；当前复杂度用 URL、局部 React 状态和集中 API 层即可解决。

## 4. Agent 与工作流状态

- 后端状态是唯一事实来源。前端统一映射为空闲、提交中、运行中、等待人工校对、等待故事确认、成功、失败可重试、已跳过和未知状态。
- 会话 ID 和讲述者 ID进入 URL；刷新或重新打开页面后，前端重新读取后端会话，不依赖内存状态冒充恢复。
- 长任务继续采用“提交任务 + 查询任务状态”；停止等待不等于取消后端任务，界面不提供误导性的“取消任务”。
- 提交期间禁用重复操作；网络失败保留输入并提供有业务含义的重试入口。
- 云模型仍只接收人工校对后的最少必要文本，并且每次都由用户单独勾选授权；API Key 和私密原文不进入浏览器日志。

## 5. 页面、接口与产物

- `/`：家庭首页，展示当前讲述者、继续记录入口、阶段覆盖、最近故事和到期提醒。
- `/record`：老人友好的回合制记录流程，包含选择话题、照片/老物件触发、录音/上传、转写、人工校对、忠实整理和确认归档。
- `/archive`：故事时间线、音频回放、Markdown/PDF 回忆录生成与下载。
- `/family`：讲述者档案、家庭成员与关系、话题意愿、安全状态、备份和恢复演练。
- 现有 API 保持兼容；只增加刷新恢复所需的“按讲述者列出最近会话”读取接口，不复制后端业务规则。
- 文字回忆录、PDF、原始音频和照片仍由后端生成、校验并以受控接口提供。

## 6. 视觉、响应式与无障碍

- 视觉方向为“温暖的家庭档案册”：米白纸张、深褐文字、克制的朱红操作色和柔和绿色成功色；不使用娱乐化动画或表情符号。
- 老人使用场景优先：正文不小于 17px，核心按钮和触控目标至少 48px，录音入口明确，步骤一次只突出一个主要动作。
- 桌面使用侧栏/顶部工作区，手机使用底部导航；至少检查 390、768、1280 和 1440px。
- 使用语义化标题、显式 Label、键盘可达焦点、文字化状态、`aria-live` 结果提示和 `prefers-reduced-motion`。
- 麦克风拒绝、设备缺失、浏览器不支持和上传失败必须分别解释，不静默失败。

## 7. 与通用手册不同的地方

- 现有页面已使用 Tailwind CSS 3 和少量全局 CSS，因此第三阶段不升级 Tailwind 4；设计变量仍集中管理。
- 现有业务是本机单家庭模式，本阶段不提前实现登录、Cookie 会话和多家庭权限；这些属于第五阶段上线与跨代保存。
- 本地任务目前能较快完成，因此不强制引入 SSE；状态恢复继续使用后端持久化任务 + 有节制轮询。
- 阶段四才实现声音复刻、TTS 和视频念想；第三阶段的“老人友好语音交互”是可靠录音、上传、转写、回放与失败恢复。

## 8. 待确认事项

无。正式响应式 Web 已由用户确认。开发完成后只需要用户用真实手机完成麦克风、触控和易用性验收。
