# 聆年 · 第五阶段前端开发手册

> 阶段名称：5A · V0.9 前端体验重构与发布准备  
> 分支：`第五阶段分支`  
> 配套声明：`第五阶段前端技术适配声明.md`  
> 状态：第二版视觉方向已获用户认可；首页、记录、档案、原声视频和家庭管理已完成全站 UI 统一，等待用户全站视觉验收

## 零、2026-08-30 验收校正

- 已完成的首页和四步记录流程可作为功能 MVP，但不是上线 UI。
- 用户明确指出：当前界面只能用于理解流程，真实上线时“用不了”；该结论优先级高于自动化通过。
- 用户还指出讲述者看起来被固定为“林奶奶”。根因是只有一条测试数据，新增入口隐藏，且缺少讲述者资料编辑 API 和明确切换界面。
- 修正顺序：先补齐讲述者新增、编辑、切换；再重建上线级界面；全站验收通过前不进入 5B。

### 2026-08-30 全站 UI 铺开结果

- 第二版首页与记录页方向获得用户认可，并已铺开到回忆档案、原声视频和家庭管理。
- 回忆档案改为双栏故事卡，原声与正文归在同一卡片；视频制作成为档案后的唯一主要下一步，提醒和导出继续默认收起。
- 原声视频改为“选择故事 → 确认授权 → 本机制作 → 播放保存”四步，并保留全部原声授权和本机处理边界。
- 家庭管理明确分为讲述者档案、讲述意愿、家庭档案安全三层；人物编辑和切换优先，备份恢复保持低频展开。
- 已用真实本机只读数据检查桌面与 390 px 页面；隔离数据的桌面/手机核心闭环均通过。
- 当前仍属于 5A 全站视觉候选，不因自动化通过自动进入 5B；需等待用户查看并确认全站 UI。

### 2026-08-30 品牌更名

- 产品展示名称由「念念」调整为「聆年」：“聆”代表听见家人的声音，“年”代表保存一生的时间。
- 新 Logo 使用“声音轨迹 + 记忆之门”图形，项目资产位于 `frontend/public/brand/lingnian-mark-v2.png`；第一版年轮图形因小尺寸辨识度和审美问题已淘汰。
- 页面标题、侧栏、提示文案、API 标题、回忆录页脚和后续生成的视频标识统一为「聆年」。
- 为避免旧档案、恢复包和钥匙串失效，`niannian` 数据库名、本地存储键、加密信封、恢复包格式、备份格式和包名继续作为内部兼容标识保留。
- 更名后的后端 40 项、前端 20 项、生产构建、正式 Web 检查与桌面/手机核心闭环均通过。

## 一、阶段目标

不增加高风险模型能力，先把已经验证过的录音、故事、照片和原声视频重新组织成一款家人一看就会用、电脑和手机都舒服、可以进入私密内测的 V0.9 产品。

### 完成定义

- 首页和记录流程作为代表页通过用户视觉与易用性确认。
- 五个现有路由使用同一套设计变量、基础组件、状态和交互规则。
- 当前核心功能不丢失，后端接口和加密边界不被前端重构改变。
- 从首页到故事归档、从档案到原声视频均能使用真实后端跑通。
- 桌面、手机、真机、自动化和浏览器人工验收全部通过。
- 输出 V0.9 发布说明、已知问题和 5B 私密上线输入清单。

## 二、开发顺序

### 5A-1 设计底座和代表页

- 整理颜色、字体、间距、圆角、阴影、容器和状态变量。
- 建立 Button、Surface、SectionHeading、StatusNotice、EmptyState、Progress/Stepper 等轻量基础组件。
- 重构首页：一个主要动作、一个继续入口、最近故事和克制的概览。
- 重构记录页：把现有长页面重排为四步主流程，保留所有真实状态和恢复能力。
- 完成桌面与 390 px 浏览器预览后给用户验收。

### 5A-2 全站铺开

- 档案：故事与原声优先，照片关系可见，提醒/导出逐步展开。
- 原声视频：选择、授权、生成和结果分区，减少首屏说明堆叠。
- 家庭管理：人物和意愿优先，安全、备份、恢复继续作为清楚但低频的设置。
- 统一加载、空、错误、禁用、等待、成功、历史和恢复状态。

### 5A-3 联调与发布准备

- 用隔离数据库跑真实前后端闭环，不读取正式家庭资料。
- 用正式本机数据库做只读浏览器检查，不擅自创建授权、上传素材或改动家庭设置。
- 完成响应式、无障碍、媒体、下载、刷新恢复、重复提交和 Console 检查。
- 输出 V0.9 发布说明和 5B 部署清单。

## 三、页面信息架构

### 首页 `/`

1. 当前讲述者与隐私状态；
2. “开始/继续一段回忆”唯一主要按钮；
3. 最近保存的故事；
4. 已确认故事、覆盖阶段、待继续记录的轻量概览；
5. 到期提醒仅在存在时出现。

### 记录 `/record`

```text
1 选择回忆入口
2 留下声音或照片
3 校对原话
4 确认故事
```

- 当前只展开正在进行的一步；已完成步骤可回看，未到达步骤不堆满操作。
- 状态来自后端会话和任务，刷新后恢复到真实当前步骤。
- “这次不想讲”始终可见但不是危险主按钮；执行前说明会清理什么。

### 档案 `/archive`

- 故事标题、正文摘要、确认时间、原声音频和关联照片构成主要内容。
- 无照片时显示克制占位，不伪造图像。
- 原声视频念想作为故事集合的自然下一步，不与提醒/导出争抢首屏。

### 原声视频 `/keepsake`

```text
选择故事 → 明确授权 → 本机制作 → 播放/下载
```

- 原声、本机、非实时影像边界保持可见，但压缩为易读说明。
- 运行状态和结果高于历史版本；生成完成后只突出播放和下载。

### 家庭管理 `/family`

- 第一层：讲述者、家庭成员、关系和话题意愿；
- 第二层：本机加密、恢复包、备份与恢复演练；
- 危险或低频操作不进入首页和手机首屏。

## 四、页面状态矩阵

| 页面/模块 | 加载 | 空 | 运行/等待 | 成功 | 失败 | 恢复 |
|---|---|---|---|---|---|---|
| 首页 | 档案骨架 | 建立讲述者 | 有未完成会话时显示继续 | 最近故事 | 本机服务提示 | 重新读取讲述者与会话 |
| 记录 | 会话骨架 | 选择入口 | 转写/整理/人工确认 | 已归档 | 可理解错误与重试 | URL + 会话 ID |
| 档案 | 故事骨架 | 去记录第一段 | 无长任务主区 | 故事/音频/照片 | 媒体失败不吞故事 | 后端重新读取 |
| 原声视频 | 目录骨架 | 无可用原声故事 | 排队/合成/授权 | 播放下载 | 重试或阻止损坏文件 | URL + 念想 ID |
| 家庭管理 | 配置骨架 | 建立家庭 | 等待恢复验证 | 安全状态清楚 | 不暴露内部堆栈 | 后端安全元数据 |

## 五、工程实施

- 保留路由层，新增 `components/ui` 与按页面/功能拆分的组件。
- 先抽取真实重复模式，不建立十几个参数的万能组件。
- `workspace-app.tsx` 采用逐块迁移，不一次性改写所有业务函数。
- API 调用仍集中在 `lib/api.ts`；状态映射继续集中在 `lib/workflow`、`lib/keepsake`。
- 不在浏览器保存长期令牌、密钥或任务结果；Local Storage 只保存非敏感的当前讲述者/会话定位。

## 六、测试与验收

### 自动化

- 组件：主按钮层级、步骤展开、状态文案、失败恢复、移动导航。
- E2E：桌面和 390 px 跑通记录归档与原声视频真实闭环。
- 回归：39 项后端测试与现有前端测试不得减少。
- 静态门槛：Lint、TypeScript strict、生产构建、无障碍扫描。

2026-08-30 当前结果：后端 40 项通过；前端 20 项单元测试通过；TypeScript、Lint、生产构建通过；正式 Web 检查 8 项通过、4 项按设备条件跳过；桌面与手机核心闭环 2 项通过。

### 人工

- 代表页先验收：首页、记录页；确认后再铺开。
- 正式数据只读检查：页面结构、真实长度、音频/视频播放和无横向溢出。
- 真机检查：麦克风、底部导航、软键盘、播放、下载和刷新恢复。

## 七、风险与回退

- 风险：视觉重构影响现有长流程。措施：先提取状态与展示组件，业务函数和 API 契约保持不变。
- 风险：为了“少点击”隐藏必要授权。措施：减少重复说明，但保留每次云外发、原声和危险操作的明确确认。
- 风险：大文件重构难以回归。措施：按代表页、全站、联调三个里程碑独立提交。
- 回退：第五阶段分支可随时回到 `第四阶段分支` 的 `f5f6f05`，不改写前四阶段历史。

## 八、5B 私密上线前置条件

5A 验收通过后，再读取上线部署手册并确认：

- 快速演示版还是正式生产版；本项目默认推荐正式私密生产版；
- 云平台和账号归属、实名与付费责任；
- 域名、HTTPS、前后端地址和同源代理；
- SQLite/对象存储/数据库迁移方案；
- 家庭登录、邀请、撤销、指定后代与继承规则；
- 云备份是否包含真实录音、照片和视频，以及逐类授权；
- 监控只收技术元数据，不上传家庭内容。
