# 念念 · 第三阶段开发手册

> 阶段名称：正式响应式 Web 端  
> 分支：`第三阶段分支`  
> 配套文档：`niannian.md`、`通用技术栈手册.md`、`前端技术栈手册.md`、`前端技术适配声明.md`  
> 状态：已确认，进入开发

## 一、阶段目标

把前两阶段用于验收的单页工具升级为老人和子女都能理解、能在电脑与手机浏览器完成核心任务的正式 Web 产品。

本阶段完成后，用户可以从家庭首页进入一次回忆记录，完成录音、本地转写、人工校对、忠实整理和确认归档；刷新或中断后可以继续；也可以查看故事、维护家庭资料并理解安全与备份状态。

### 完成定义

- 正式 Web 的四个主要入口和统一导航完成。
- 录音、上传、转写、校对、归档、时间线、家谱、提醒、回忆录和安全能力均通过真实后端联调。
- 加载、空、失败、等待人工操作、成功、未知状态和刷新恢复都有明确界面。
- 390、768、1280、1440px 无意外横向滚动，核心操作触控目标满足老人使用。
- 键盘流程、焦点、Label、状态播报、颜色对比和减少动画基础验收通过。
- 后端测试、前端单元/组件测试、Playwright E2E、lint、类型检查和生产构建通过。
- 用户在真实手机上完成录音、上传、回放和核心流程易用性验收。

### 本阶段明确不做

- 微信入口、小程序、原生 App；
- 声音复刻、TTS 播报、照片驱动视频和数字人；
- 登录、多人远程权限、公开分享、外部消息推送；
- 公网部署、生产对象存储、云数据库和正式监控；
- 改写已经验收通过的加密与恢复模型。

## 二、技术适配摘要

- 目标终端：正式响应式 Web。
- 开发路径：在现有纵向切片上扩展，不建设平行临时页面。
- 前端：Next.js App Router + React + TypeScript strict + Tailwind 3 + 语义化 CSS 变量。
- 后端：保留 FastAPI `/api/v1` 契约，只增加恢复页面所需的只读会话列表。
- 状态：后端持久化任务为事实来源，URL 保存讲述者和会话定位，短轮询恢复运行任务。
- 测试：pytest + Vitest/RTL + Playwright 隔离 E2E + 浏览器人工检查。

## 三、信息架构与用户流程

### 3.1 页面结构

| 页面 | 用户目标 | 主要内容 |
|---|---|---|
| 家庭首页 `/` | 看懂今天能做什么 | 当前讲述者、继续记录、阶段覆盖、提醒、最近故事 |
| 开始记录 `/record` | 完成一段回忆 | 话题/触发物、录音/上传、转写、校对、整理、确认 |
| 回忆档案 `/archive` | 回看与保存成果 | 时间线、音频、回忆录版本、Markdown/PDF 下载 |
| 家庭管理 `/family` | 维护人物、意愿与安全 | 档案、家谱、关系、话题偏好、加密、备份恢复 |

### 3.2 核心流程

1. 进入首页并选择讲述者。
2. 点击“开始记录一段回忆”。
3. 选择人生阶段，或用照片/老物件触发；被设为“不要再问”的话题不可进入。
4. 授权麦克风后录音，或上传已有音频；上传前可试听和重录。
5. 提交本地转写，看到真实任务状态。
6. 人工校对；未保存时离开页面会提醒。
7. 选择仅本地整理或单次授权云模型，生成忠实草稿。
8. 退回修改或确认归档；归档后在回忆档案中可见。
9. 刷新含 `elder`、`session` 参数的 URL，恢复到后端真实步骤。

## 四、页面状态矩阵

| 模块 | 加载 | 空 | 运行/提交 | 等待用户 | 成功 | 失败 | 恢复 |
|---|---|---|---|---|---|---|---|
| 应用启动 | 骨架和“正在读取家庭档案” | 引导创建档案 | 不适用 | 不适用 | 进入首页 | 显示重试且不伪装为空 | 重新请求健康和档案 |
| 记录流程 | 读取会话 | 引导选择话题 | 禁止重复提交，显示真实步骤 | 校对、授权或确认 | 显示下一步 | 保留录音/文字并解释重试 | URL 读取会话和任务 |
| 时间线 | 故事骨架 | 明确“还没有已确认故事” | 回忆录生成中 | 不适用 | 故事/下载可用 | 单项失败不覆盖现有成果 | 刷新后重新读取 |
| 家庭管理 | 分区骨架 | 分别显示人物/关系空状态 | 保存中禁用对应操作 | 删除和启用前确认 | 局部状态更新 | 局部错误不清空其他区 | 重新拉取后端状态 |
| 录音 | 请求权限 | 无录音提示 | 录制中计时 | 试听、重录或发送 | 文件可上传 | 区分拒绝/无设备/不支持 | 已上传媒体从会话恢复 |

## 五、接口契约增量

### 5.1 新增读取接口

`GET /api/v1/elder-profiles/{profile_id}/memory-sessions?limit=20`

- 用途：首页显示可继续的最近会话，刷新或更换页面后恢复工作流。
- 返回：按更新时间倒序的 `MemorySessionRead[]`。
- 校验：`limit` 为 1～100；讲述者不存在返回统一 `ELDER_NOT_FOUND`。
- 安全：只返回现有会话读取结构，不包含密钥、恢复口令或未授权额外原文。

### 5.2 保持兼容的现有接口

- 档案、人物关系、题库偏好、记忆覆盖；
- 创建/读取/跳过会话；
- 音频和图片上传、媒体读取；
- 转写任务、重试、校对、整理、确认；
- 时间线、提醒、回忆录；
- 安全状态、备份和恢复演练。

### 5.3 轮询规则

- 创建任务成功后按任务 ID 每 1 秒查询；页面不可见时暂停主动轮询。
- `succeeded`、`failed_retryable` 或终态后立即停止。
- 连续网络失败采用退避并显示“连接中断，任务可能仍在运行”；恢复后先读任务状态，不重复创建任务。

## 六、前端实现结构

```text
frontend/
├── app/
│   ├── page.tsx
│   ├── record/page.tsx
│   ├── archive/page.tsx
│   ├── family/page.tsx
│   ├── loading.tsx
│   └── error.tsx
├── components/
│   ├── ui/
│   └── layout/
├── features/
│   ├── profile/
│   ├── recording/
│   ├── workflow/
│   ├── archive/
│   └── family/
├── lib/
│   ├── api/
│   ├── media/
│   └── workflow/
├── tests/
└── e2e/
```

现有代码按业务闭环逐步迁移，先抽取 API、类型和状态映射，再拆页面；每一步保持可运行，不进行一次性推倒重写。

## 七、设计系统与交互规则

- 主色：克制朱红；成功：深绿；背景：温暖米白；危险操作同时使用图标/文字，不只依赖颜色。
- 中文正文使用系统字体 `PingFang SC` 等，标题可使用宋体风格；不打包商业字体。
- 核心正文 17～19px，行高不低于 1.65；主操作目标至少 48×48px。
- 桌面左侧导航，移动端底部导航；当前页面有文字和 `aria-current`。
- 每个页面只有一个最突出的主操作；高级安全能力放在家庭管理中，不与老人录音入口竞争注意力。
- 动画只用于状态过渡，且尊重 `prefers-reduced-motion`。

## 八、数据、安全与恢复

- 前端不持久化密钥、恢复口令、API Key、完整转写或故事原文。
- URL 只保存非秘密 ID；用户偏好可保存当前讲述者 ID，但打开页面后必须向后端重新验证。
- 录音 Blob 只在当前页面内存在，生成的 Object URL 在替换或卸载时释放。
- 删除、跳过、真实资料启用、云模型授权和完整恢复演练继续由后端确定性校验。
- 未保存校对文字离开时提醒；后端已保存的录音、转写、草稿和任务可通过会话 ID 恢复。

## 九、开发顺序

### 里程碑 A：正式骨架与代表首页

- 设计变量、应用外壳、桌面/移动导航、讲述者选择、首页摘要；
- API 错误归一化、全局加载/错误和空状态；
- 会话列表读取接口与首页“继续记录”。

### 里程碑 B：老人友好记录闭环

- 话题与照片触发；
- MIME 探测、麦克风权限、录音计时、试听、重录、上传；
- 转写任务状态、校对未保存保护、云模型单次授权、整理与确认。

### 里程碑 C：档案与家庭管理

- 时间线、音频回放、回忆录生成下载；
- 档案、家谱、关系、话题意愿；
- 安全与备份折叠区，保留第二阶段完整能力。

### 里程碑 D：质量与恢复

- 断网、失败、重试、刷新恢复、防重复；
- 390/768/1280/1440 响应式；
- 键盘、焦点、Label、live region、对比度和减少动态效果；
- 自动测试、隔离 E2E 和真实浏览器验收。

## 十、测试要求

### 10.1 后端

- 现有 pytest 全量回归；
- 新会话列表的顺序、limit、空状态和不存在档案；
- 运行中/失败可重试任务能被列表和详情恢复。

### 10.2 前端单元与组件测试

- API 错误归一化和超时；
- 后端状态到用户文案的映射及未知状态；
- 录音 MIME 选择、权限拒绝和 Object URL 清理；
- 导航、空/错/加载、提交禁用和未保存提示；
- 关键表单 Label、可访问名称和状态播报。

### 10.3 Playwright E2E

- 使用隔离 SQLite 和资产目录，不写入用户正式家庭资料；
- 建测试档案 → 创建会话 → 上传测试音频 → 转写 → 校对 → 整理 → 确认 → 时间线可见；
- 刷新记录页后按 URL 恢复同一会话；
- 390px 和 1280px 完成核心路径且无横向溢出；
- 重复点击不会创建重复任务，后端错误可见且保留输入。

### 10.4 人工浏览器与真机

- Chrome 桌面检查全部页面、Console、Network 和媒体下载；
- 390、768、1280、1440px 检查导航、弹窗、表单、媒体和滚动；
- 用户用真实手机（含 iPhone 时优先 Safari）验收麦克风授权、录音、试听、上传和触控；
- 真机麦克风需要 HTTPS 或 localhost 安全上下文，届时提供一步一步的本机 HTTPS 验收方法。

## 十一、验收清单

- [ ] 我打开首页就知道当前为谁记录、今天可以做什么。
- [ ] 我在电脑和手机上都能找到“开始记录”。
- [ ] 麦克风拒绝、无设备和不支持时，我能看懂怎么处理。
- [ ] 我能录音、试听、重录并提交，不会因连点产生重复任务。
- [ ] 我能校对文字，未保存离开时会被提醒。
- [ ] 我能明确决定是否把本次校对文字交给云模型。
- [ ] 刷新页面后，已上传录音、转写和草稿仍然存在。
- [ ] 我能确认故事并在回忆档案中看到它。
- [ ] 我能维护家庭成员、关系和“不愿再问”的话题。
- [ ] 我能理解本机加密、备份和恢复状态。
- [ ] 键盘可以完成核心操作，文字和按钮对老人足够清楚。

## 十二、风险与回退

- iOS/Safari 录音格式可能与 Chrome 不同：运行时探测 MIME，后端按真实内容校验；不支持时允许上传已有音频。
- 手机访问 Mac 本机服务时麦克风需要 HTTPS：自动验收先覆盖桌面安全上下文，最终由用户按指引做一次真机验证。
- 当前页面功能集中、拆分风险较高：采用增量迁移，每个里程碑都跑全量回归；`第二阶段分支` 保留可随时回退的完整快照。
- 第三阶段不解决公网身份与远程分享；页面仍明确标注为本机家庭档案。

## 十三、交接给第四阶段

第三阶段结束后，第四阶段直接复用正式 Web 外壳、媒体组件、长任务状态、人工确认、刷新恢复和加密资产接口，新增授权声音采集、声音留声与视频合成，不再重建基础前端。
